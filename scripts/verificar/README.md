# O arnes de verificacao

Utilitarios de linha de comando (Node + Playwright) para conferir a ferramenta
e os blocos que ela gera. Nada aqui e servido nem colado no Prosite -- e
material de apoio, roda no seu computador.

## O que ha aqui

| Arquivo | O que e | Quando usar |
|---|---|---|
| `lib.mjs` | Helpers comuns: achar o Playwright, subir um servidor estatico, abrir a ferramenta com armazenamento limpo, preencher campo/checkbox/radio disparando os mesmos eventos do teclado, ler saida, capturar alerta/erro de console. | Nunca sozinho -- e a base dos outros arquivos. Leia antes de escrever qualquer coisa nova neste diretorio. |
| `cenario.mjs` | O CENARIO: o que se preenche em cada uma das dez abas antes de gerar (identidade de teste, produtos, pacotes, familias, cupons, cobranca) e a tabela `TEXTOS`, com os 28 campos de texto da passagem configurada. Nao mede nada e nao abre navegador. | Ao acrescentar aba, campo ou texto que precise entrar na prova. E o unico lugar onde o cenario existe -- `geradores.mjs` e `textos-escape.mjs` leem os dois o mesmo. |
| `geradores.mjs` | Fotografa o TEXTO que as dez abas produzem, em DUAS passagens (`fabrica` e `configurada`), mais o link completo de cada cenario de cobranca, e grava num JSON. | Para provar que uma mudanca no gerador nao alterou um byte do que as outras abas produzem -- e que o texto configurado pelo dono chega ao bloco. Chamado por `regressao.sh`; raramente direto. |
| `regressao.sh` | Compara a fotografia da arvore de trabalho com a de uma referencia (`main` por padrao). | Ao fim de toda rodada que mexer em codigo gerado. `scripts/verificar/regressao.sh` ou `scripts/verificar/regressao.sh <ref>`. |
| `textos-escape.mjs` | Pega os blocos gerados COM os textos de escape (aspas simples e duplas, barra invertida, acento e `</script`) e os EXECUTA numa pagina de verdade, uma por bloco. Checa que nenhum `</script` engoliu o resto do documento, que o bloco desenhou, que nao houve erro de console e que o texto do dono chegou inteiro a tela. | Ao mexer em qualquer escape (`esc`, `escJs`, `escJsD`, `escAttr`, `aTplJs`) ou em qualquer texto configuravel. `node scripts/verificar/textos-escape.mjs`. |
| `textos-sinal.mjs` | **OS TRES TEXTOS QUE EXPLICAM O SINAL** (13/09/2026, rodada E). Doze campos novos -- tres por aba de pagamento -- que respondem, na tela do cliente e **antes dos botoes de pagamento**, as tres perguntas do pedido do dono: o que o sinal garante, e se ele desistir, o que fazer com o saldo. Eles **nascem vazios** (sao declaracoes de politica comercial: um padrao de fabrica afirmaria, para um cliente pagante, uma politica que o dono pode nao ter) e **linha vazia nao aparece** -- e e exatamente esse desenho que deixa a regressao byte a byte **cega** para eles: com a fabrica a saida e identica a de antes da rodada, entao `regressao.sh` prova que nada quebrou e nao consegue provar que algo passou a funcionar. Esta suite e o outro lado. **161 verificacoes, quatro passagens pela ferramenta** (tres vazios / sem sinal / um preenchido / os tres) e **nove execucoes de bloco**. **1. Os tres estados, com o bloco rodando:** tres vazios -> nada no DOM; um preenchido -> so ele (e o do MEIO, para um gerador que contasse indice em vez de conteudo ser pego); os tres -> os tres, na ordem das perguntas. **2. A POSICAO, pela ordem no DOCUMENTO** (`compareDocumentPosition`) e nao pela aparencia: depois das linhas de sinal e saldo, antes do primeiro controle de pagamento -- "antes" e uma afirmacao sobre estrutura, e medir por coordenada mediria o CSS. **3. So com o sinal ligado**, medido no TEXTO do bloco para distinguir "nao foi emitido" de "foi emitido e nao apareceu". **A cobranca e medida de outro jeito, e a diferenca e o desenho daquela aba, nao um caso especial do teste:** la o sinal e de cada COBRANCA e viaja no link, entao o bloco carrega os textos SEMPRE -- exigir ausencia no texto quebraria a propriedade que sustenta o `naoEmite` dela. A prova equivalente e o bloco rodando sobre um link SEM sinal, sem desenhar linha nenhuma. **4. O aviso ambar** nas quatro abas, em cinco estados cada (sem sinal / sinal com os tres vazios / um preenchido / so espaco em branco / sinal desligado de novo), medido por **visibilidade real da caixa** e nunca por `getComputedStyle` -- a armadilha ja registrada em `sinal.mjs`. **5. Texto hostil nos doze** (apostrofa, aspas duplas, barra invertida, acento, `&`, `<b>` e `</script`), com o bloco executando e a medida sendo o **documento inteiro sobreviver**. **6. A busca acha os doze** com eles VAZIOS (pelo rotulo e pelo id -- que e como o dono vai procurar na primeira vez) e pelo VALOR depois de preenchido, e o **eco do rotulo** nasce vazio e passa a mostrar o valor atual. **7. O contrato do campo:** os doze nascem com `value=""` **escrito no HTML** -- sem o atributo, `getAttribute('value')` devolve `null`, `null !== ''` e `fcTxtFabricaDiverge` acenderia a barra vermelha --, com o exemplo do dono no `placeholder` e o rotulo escrito como a pergunta. **Duas provas sobre o LINK de cobranca** fixam que os textos sao **da pagina** e nao da venda: o link sai **identico** com um e com tres preenchidos (se um dia entrarem no selo, esta linha cai -- e e o aviso que se quer, porque parametro novo no link e recusado por toda `/pagar` com codigo 1 antigo), e o bloco, ao contrario, muda. | Ao mexer em `FC_SINAL_TXT`, em `fcSinalTxtSrc`/`fcSinalTxtLista`/`fcSinalTxtAviso`, nos doze campos `*-txt-sinal-*` do HTML, ou ao levar os tres textos para uma aba nova. Nao precisa de internet. `node scripts/verificar/textos-sinal.mjs`. |
| `cupom-minimo.mjs` | O VALOR MINIMO DO PEDIDO PARA O CUPOM VALER, com os blocos RODANDO: gera as tres abas que tem cupom (Checkout, Mini loja e Agendamento por pacote) com um cupom de minimo e um sem, executa cada bloco numa pagina e percorre os cinco casos -- aplicado acima, recusado abaixo, a QUEDA AUTOMATICA ao tirar um item, o voltar a subir que nao reaplica, e o cupom sem minimo intacto. Le o total NA TELA, nunca a variavel. Fecha com a compatibilidade: estado de versao anterior, "Exportar tudo"/importar e preset de aba. | Ao mexer em qualquer coisa do cupom nas tres abas -- a regra do minimo, `FC_CARRINHO_SRC`, `fcCpSerial` ou os dois textos novos. `node scripts/verificar/cupom-minimo.mjs`. |
| `lista-cupons.mjs` | O EDITOR EM LINHA da lista de cupons, nas TRES abas que tem cupom. Para cada um dos cinco campos da linha (codigo, tipo, valor, validade, minimo): que ele APARECE, que edita-lo GRAVA (le o `localStorage`), que edita-lo REMONTA A PREVIA (le a lista `CUPONS` de dentro do iframe) e que o valor sobrevive a recarga. | Ao mexer em qualquer `*CpRender`, ou ao criar lista cadastrada com campo editavel na propria linha. A regressao NAO alcanca isto: os campos da linha sao criados por JavaScript, nao tem id nem name, e escapam dos ouvintes delegados de cada aba -- foi assim que a Mini loja passou a mentir na previa e a esconder a validade sem quebrar nada. `node scripts/verificar/lista-cupons.mjs`. |
| `pac-quantidade.mjs` | A QUANTIDADE DO OPCIONAL NO QUE VAI PARA O PAYPAL, na aba Agendamento por pacote, com o bloco RODANDO. Intercepta o `document.head.appendChild` do proprio bloco (o mesmo caminho da sonda da previa, `fcPvSondaPP`), instala um `window.paypal` que guarda a configuracao dos Buttons e chama o `createOrder` do bloco com um `order.create` que so devolve o pedido. Le o `name`/`description` que iriam a ordem nos quatro casos (com quantidade, sem quantidade, os dois juntos, so o pacote) e o corte em 127 caracteres. | Ao mexer em `nomesSelecionados` de qualquer aba, ou em `fcPpBotoesSrc` -- que e a fonte unica do que o PayPal recebe. A regressao NAO alcanca isto: ela compara texto gerado, e o defeito que este arquivo pegou (preco multiplicava pela quantidade, nome nao dizia por quanto) era texto gerado coerente e recibo errado. `node scripts/verificar/pac-quantidade.mjs`. |
| `preset-formulario.mjs` | O FORMULARIO DE CADASTRO nos DOIS caminhos que passam pelo `restaura()` de cada aba, e que querem coisas opostas: **recarregar** a ferramenta tem de DEVOLVER o formulario inacabado (opcionais digitados, item em edicao e, desde 03/09/2026, os CAMPOS DE TEXTO dele), e **aplicar um preset** tem de ZERA-LO. Nas tres abas com catalogo (Checkout, Mini loja, Agendamento por pacote), em tres partes: **A.** monta o cenario, mede o estado ANTES do clique, aplica, confere que os 6/8/7 campos do formulario voltaram ao padrao, e fecha com a prova que importa -- cadastrar logo depois ACRESCENTA em vez de sobrescrever o item de indice 1. **B.** recarrega e depois DIGITA POR CIMA dos campos sem salvar e recarrega de novo: todos os campos medidos em `*ProdSalvar`/`aPacSalvar` voltam com o que foi digitado, e o rotulo do botao mais o aviso de pendencia continuam falando do MESMO item (campo restaurado com o botao dizendo outra coisa e pior que campo perdido). **C.** apaga a chave `form` do estado gravado -- que e exatamente o que um backup anterior a 03/09/2026 tem -- e confere que a ferramenta abre no comportamento de ontem, sem alerta, sem barra vermelha e sem erro de console; na aba Agendamento por pacote, mais o caso vizinho da FAMILIA que nao existe mais, que nao pode deixar o seletor em branco. | Ao mexer em `fcPresetAplicar`, `fcgAplicarAba`, `restaurarEstado`, em qualquer `*EditRestaurar`/`*ProdLimpar`/`*PacLimpar`, em `fcFormLer`/`fcFormRestaura`/`U_FORM`/`M_FORM`/`A_FORM`, ou nas listas `fora`/`formulario` de `ABAS`. A regressao NAO alcanca isto: e interface, e os dois defeitos (03/09/2026) eram silenciosos -- um gravava por cima de um produto sem erro de console e sem alerta, o outro perdia tudo o que o dono tinha digitado no formulario a cada recarga. Medido na `main` de 03/09/2026: 6 das 98 verificacoes falham la, uma por aba mais a que confere que a chave `form` passou a ser gravada. `node scripts/verificar/preset-formulario.mjs`; um caminho como argumento aponta outra arvore (um `git worktree` da referencia) para ver o defeito falhar la. |
| `tidycal-origem.mjs` | O **filtro de origem** das mensagens do TidyCal, com os dois blocos RODANDO, mais a guarda do campo e a migracao. Cinco partes: dominio proprio (o iframe aponta para ele e o sinal vindo DELE e aceito), tidycal.com (continua como sempre), o **negativo** em cada um -- sinal de outra origem e descartado --, dois pacotes em **dominios diferentes** na mesma pagina (escolhe um, mede os dois sentidos, troca, mede de novo), a tabela de 20 casos do que o campo aceita e recusa (exercitada pelo botao "Abrir a pagina de teste", nao pela funcao), e a migracao das tres formas gravadas (`link` -> `path` -> `cal`), com o aviso saindo uma vez e nao repetindo na recarga. Os tres primeiros rodam **nos dois valores** do campo `t-medir`/`a-medir`, porque o que denuncia "o sinal foi aceito" muda com ele: no modo calibrado e o `min-height` que `expandir()` escreve; no modo que mede (o padrao) `expandir()` nao faz nada, e quem denuncia e a **altura** que veio no proprio sinal. O sinal de outra origem e simulado com `new MessageEvent(..., {origin})`: `postMessage` sempre carimba a origem da propria pagina. **Desde 04/09/2026 ele le o quadro VISIVEL (`VIS`), e nao "o primeiro iframe do documento"**: a vitrine passou a manter um quadro por pacote visitado, e ler o primeiro mediria o quadro do pacote ANTERIOR -- foi assim que quatro verificacoes deste arquivo passaram a acusar sozinhas. O invariante que ele cobra mudou junto: era "um iframe so, reapontado", e virou "um quadro por pacote visitado, e todos os que nao sao o da vez escondidos". | Ao mexer no endereco do TidyCal de qualquer das duas abas, no filtro de origem dos blocos, em `fcTidyUrl`/`fcTidyUrlOk`/`fcTidyOrigem` ou na migracao de `aRestaura`/`tRestaura`. A regressao NAO alcanca isto: uma origem cravada e texto gerado perfeitamente coerente, e o defeito seria mudo -- o calendario apareceria e a expansao do formulario nunca aconteceria. `node scripts/verificar/tidycal-origem.mjs`. |
| `tidycal-altura.mjs` | A **ALTURA AUTOMATICA DO CALENDARIO**, nos dois blocos que criam o proprio iframe (aba TidyCal em dominio proprio e Agendamento por pacote). Espia o `contentWindow.postMessage` do bloco para provar que o **aperto de mao** sai, com o formato medido e para a origem certa; corre uma bateria de sinais TRANSCRITOS da medicao real (75, 507, 553, 716, 770) e prova os tres casos: a altura acompanha o conteudo, fica na de partida quando o sinal nao chega, e valor fora da faixa (50000, negativo, nao-numero, `:0:0:`) nao passa. Cada bloco corre a bateria **duas vezes, uma por valor** do campo `t-medir`/`a-medir`: desligado, o aperto de mao sai com `bodyOffset` e o `scrollToOffset` expande por `min-height` ate a altura calibrada; ligado (o padrao), o aperto de mao sai com `lowestElement`, o `scrollToOffset` NAO expande nada, e o quadro cresce e volta pela altura anunciada (1662 -> 764, transcritos). Fecha com a troca de pacote -- altura volta para a de partida e o aperto de mao recomeca na origem nova. **Desde 04/09/2026 ele le e sinaliza o quadro VISIVEL (`VIS`)**: com um quadro por pacote visitado, ler "o primeiro iframe" media o quadro do pacote anterior, e a troca parecia nao devolver a altura de partida quando devolvia. O invariante final tambem mudou -- de "continua existindo UM iframe so" para "um quadro por pacote visitado, dois aqui, e nenhum a mais". | Ao mexer em `fcTidyAlturaSrc` (a fonte unica das duas abas), na faixa da guarda, no ouvinte de `message` de qualquer dos dois blocos, ou em `abrirCalendario`. A regressao NAO alcanca isto: ate 03/09/2026 os dois blocos escutavam `[iFrameSizer]` sem NUNCA mandar o aperto de mao -- texto gerado perfeitamente coerente, e **medido**, calendario de 700px fixos num lado e de **150px** no outro, sem erro nenhum no console. `node scripts/verificar/tidycal-altura.mjs`. |
| `tidycal-unificado.mjs` | O **CALENDARIO UNIFICADO, CONTRA O TIDYCAL DE VERDADE** -- o unico arquivo do arnes que fala com a internet, e de proposito. Gera os blocos da arvore de trabalho E os da referencia (`main` por padrao) e roda os **tres casos** lado a lado -- aba TidyCal em tidycal.com, a mesma aba em dominio proprio, e a vitrine de pacotes com dois pacotes em dominios diferentes --, medindo em cada um: a altura em repouso, a expansao quando o formulario de reserva abre, quantos iframes ha na pagina e os erros de console. **E cada caso e medido nos dois valores do campo `t-medir`/`a-medir`**: o `[calibrado]` e o que se compara com a referencia, porque e o mesmo comportamento dela; o `[medindo]` -- o padrao desde 03/09/2026 -- e medido contra os dois lados da troca, em repouso ele fica acima do calibrado (o preco) e com o formulario aberto ele cresce ate o necessario, ficando **abaixo** dos 2350 calibrados e voltando ao repouso quando o modal fecha. Fecha com os tres casos de degradacao (a biblioteca deles carrega / nao carrega / carrega e o sinal nao vem) e com **dois blocos na mesma pagina** (o `embed.js` entra uma vez so, e nenhum bloco tem os seletores que ele varre). | Ao mexer em `fcTidyCalSrc`, no bloco de qualquer das duas abas, ou no dia em que o TidyCal trocar a biblioteca deles. A regressao NAO alcanca isto: a saida dos dois blocos mudou DE PROPOSITO na unificacao, entao byte a byte so sabe dizer "mudou" -- a pergunta que importa ("o novo se comporta pelo menos tao bem quanto o de hoje?") so tem resposta com o calendario real do outro lado. `node scripts/verificar/tidycal-unificado.mjs`, ou com outra referencia como argumento. **Desde 04/09/2026 ele le o quadro VISIVEL (`SEL_VIS`)**, e nao "o primeiro iframe": com um quadro por pacote visitado, a medicao da troca lia o calendario do pacote ANTERIOR (medido: novo=1054, que e a altura do primeiro, contra atual=949, que era a do segundo). O invariante final tambem mudou -- de "nao deixa dois iframes" para "um quadro por pacote visitado, dois aqui". **FALHA CONHECIDA, INTERMITENTE, e ela nao e da arvore:** as duas verificacoes de "a altura em repouso do novo nao e pior que a do atual (ate 8px de folga)" da aba TidyCal acusam de vez em quando com `novo=559 atual=500` -- medido em 04/09/2026: aparecem numa passagem e somem nas duas seguintes, com o mesmo codigo. Os dois lados executam o MESMO texto ali (a regressao byte a byte prova que `t-out1` tem 12215 bytes identicos nas duas arvores), entao a diferenca so pode ser o servidor do TidyCal: 500 e o PISO (`ALTURA_MINIMA`), ou seja a passagem em que o calendario nao terminou de se desenhar dentro do prazo. E o limiar calibrado em 03/09/2026 envelhecendo. Quando aparecer, a leitura certa e "nao mediu". **Medido de novo em 04/09/2026, depois do conserto da corrida** (`corrida-calendario.mjs`): a suite fecha com **1 falha de 65**, e e esta mesma -- `novo=559 atual=500` no dominio proprio, com a REFERENCIA desistindo depois de tres passagens no piso. Ou seja, o lado que nao mediu foi o da referencia, e 559 e melhor que 500, nao pior. A falha do limiar de crescimento que se via em 03/09 nao apareceu nesta rodada. |
| `calendario-aquecido.mjs` | O **PRE-CARREGAMENTO DO CALENDARIO, O TAMANHO EM QUE ELE CARREGA E O PERIGO QUE VEM JUNTO** (04/09/2026). **Oito partes.** As tres primeiras sao da rodada do aquecimento: **1.** com a rede fechada, os `<link data-fc-pre>` (`preconnect` e `dns-prefetch` por origem, sem repetir entre dois blocos). **2.** o negativo do `e.source` com um quadro ISCA fabricado pelo teste, mais o positivo (sem ele, um bloco que ignorasse tudo passaria com louvor). **3.** e **3b.** contra o TidyCal de verdade: o quadro pre-carregado carregou mesmo (requisicoes sairam, o documento disparou `load`), esta escondido **com a largura real**, nao abre buraco, e revelado nao troca `src`, nao dispara `load` novo, e o no do DOM e o MESMO; e o modo `nao`, em que a pagina abre e a familia e escolhida **sem calendario nenhum**. **4.** os tempos, arvore de trabalho contra referencia, com a linha do tempo identica nos dois lados. **As quatro ultimas sao da rodada dos QUADROS POR PACOTE**, que respondeu as duas observacoes do dono: **5.** os **TRES MODOS** do campo `a-precal` medidos lado a lado contra o TidyCal real, cada um com dois alvos de clique -- o primeiro cartao (o que o modo `um` pre-carrega) e o SEGUNDO, que e "ir direto num pacote que nao e o pre-carregado" --, com tempo, sinais crus e **memoria** (a soma do RSS dos processos do Chromium do Playwright; o Browser desta versao nao expoe `.process()`, entao o pid nao existe para ser seguido). **6.** a **observacao 2 do dono**: familia 1 -> pacote -> familia 2 -> pacote dela -> DE VOLTA, contando `load` novos e trocas de `src` na volta; zero nos dois e a prova. **7.** a **largura de cada quadro** e o buraco que nao existe, nos tres modos e com a rede fechada: nenhum quadro com largura 0, nenhuma caixa com altura, e a altura rolavel da pagina identica nos tres. **8.** o `e.source` com os quadros **DE VERDADE**: no modo `todos` ha tres na pagina e dois na MESMA origem -- o filtro de `e.origin` nao separa um do outro, quem separa e `doQuadro`. **9.** o **CAMPO** `a-precal` na propria ferramenta, sem rede: o padrao de fabrica, os tres valores chegando ao bloco, a recarga, o preset da aba, e os dois estados gravados que nao dizem nada (backup anterior a esta versao, sem a chave; e valor invalido) caindo no padrao sem deixar o radio em branco. **Medido em 04/09/2026** (Chromium do Playwright, rede local): modo `nao` primeiro cartao 2132ms / altura assentada em 3448ms / 288MB; modo `um` 0ms / 157ms / 386MB; modo `todos` 0ms / 150ms / 452MB. Indo DIRETO no segundo cartao: `nao` 2537ms, `um` 388ms (o custo unico ja foi pago), `todos` 0ms. Voltar a um pacote ja visitado: **0 loads e 0 trocas de src**. Largura durante a pre-carga: **960px em todos os quadros**, nos tres modos. `animationstart`: **zero em todo quadro pre-carregado**; o unico medido esta num quadro que nasceu NO clique -- primeiro desenho, e nao redesenho (o modo `nao` tem o mesmo desenho e nao o registra so porque a biblioteca de altura ainda esta baixando quando a animacao roda). | Ao mexer em `preConectar`, `doQuadro`, `criarQuadro`/`mostrarQuadro`/`ativar`, em `aquecer`/`aquecerCartao`/`primeiroDaFamilia`, em `abrirCalendario`/`escolher`, no campo `a-precal`, ou no CSS que esconde o calendario (`.fca-aquec` e `.fca-quadro.fca-oculto`). A regressao NAO alcanca isto: ela diz que `a-out1` mudou, o que ja se sabia. "Carregou?", "carregou do TAMANHO CERTO?", "quanto custou em tempo e em memoria?" e "voltar recarrega?" so tem resposta com o calendario real do outro lado, e "a altura do quadro errado e ignorada?" so tem resposta com varios quadros vivos ao mesmo tempo. `node scripts/verificar/calendario-aquecido.mjs`, ou com outra referencia como argumento. **Ele PODE falhar por rede nas partes 3 a 6**, e quando falhar a leitura certa e "nao mediu", nao "quebrou". **Um conflito PRE-EXISTENTE, que este arquivo aprendeu a nao confundir com o defeito dele -- e que foi CONSERTADO em 04/09/2026, ver `corrida-calendario.mjs`; o texto abaixo fica porque explica por que a leitura degradada continua aqui:** numa passagem em tres o `embed.js` do TidyCal chega DEPOIS de o aperto de mao proprio ja ter assumido o filho; a biblioteca deles e entao aplicada ao elemento por cima e o prende no piso de 500px (`minHeight`), enquanto o calendario continua anunciando a altura certa (medido: 1021 escondido, 1054 revelado). O conflito e da fonte compartilhada (`fcTidyCalSrc`), vale igual na aba TidyCal e ja estava registrado -- `tidycal-unificado.mjs` tem uma SEGUNDA CHANCE inteira escrita para ele, com este mesmo numero. Por isso a parte 3 compara as duas alturas ANUNCIADAS pelo proprio calendario (escondido contra revelado), que e o que ela quer medir e nao depende de quem escreve no elemento, e degrada a leitura do elemento para "medicao, nao assercao" quando ela cai em 500 ou 700. A altura do elemento continua cobrada de verdade nas partes 5 e 6. **Depois do conserto de 04/09/2026** a corrida nao acontece mais (medido: a biblioteca era aplicada por cima em 9 de 9 passagens forcadas, e passou a 0 de 9), mas a leitura degradada FICA: ela tambem cobre o outro caso em que 500 ou 700 aparecem, que e o calendario nao terminar de se desenhar dentro do prazo -- e esse nao tem conserto do nosso lado. |
| `acentos.mjs` | O ACENTO QUE FALTA no texto que uma PESSOA le. Tokeniza o `index.html` de verdade (string, comentario e codigo separados), recolhe as frases que chegam a um sink de interface (`alert`, `confirm`, `.textContent`, `.placeholder`, `.title`, `.alt`, `setAttribute('aria-label')`, `fcFalha`, `fciRecusa`, o texto e os atributos do HTML da ferramenta) mais todo literal que PARECE PROSA, e cobra acento comparando com o VOCABULARIO acentuado da propria arvore. Nao acusa codigo: comentario, `<style>`, `<code>`, marcador `{codigo}`, identificador, classe CSS, `value` de radio, comentario do bloco gerado e a lista declarada de HOMOGRAFOS (`e`/`é`, `esta`/`está`, `pode`/`pôde`...). Sai com codigo 1 quando acha algo. | Ao escrever qualquer texto novo que uma pessoa va ler -- e como guarda, antes de fechar a rodada. `node scripts/verificar/acentos.mjs`; `--vocabulario` mostra o que ele aprendeu; um caminho como argumento analisa outro arquivo. Medido em 03/09/2026: 118 frases na `main`, zero depois da rodada dos acentos. |
| `duplicar-itens.mjs` | OS BOTOES DE DUPLICAR de toda a ferramenta, e o que eles nunca podem sobrescrever ou apagar. Casos 1 a 7, aba Agendamento por pacote: duplicar um pacote COM OPCIONAIS (o formulario vem completo, em modo criar, e salvar cria um SEGUNDO pacote -- o original e comparado campo a campo antes e depois), duplicar ENQUANTO EDITA outro pacote (o que estava em edicao fica intacto), duplicar e RECARREGAR sem salvar (o estado persistido devolve a copia ainda em modo criar, com os SETE campos de texto dela, e SALVAR depois da recarga cria a copia deixando os dois pacotes do cenario intactos campo a campo), duplicar um OPCIONAL (a copia entra logo abaixo da original, com preco e "vende por quantidade" iguais), a RECUSA DE CODIGO REPETIDO (no cadastro e na geracao, inclusive o caso que so o Pix enxerga -- "MINI-1H" x "MINI1H" --, e o negativo: salvar uma edicao sem mexer no codigo nao pode ser recusada), o ARRANJO dos botoes (a faixa embaixo do item, os cinco botoes, e o simbolo do duplicar que nao pode ser um retangulo vazio na fonte do sistema) e o AVISO DE PENDENTES citando o rotulo que esta na tela. Casos 8 a 15, acrescentados na leva 3 de 03/09/2026: duplicar um PRODUTO no Checkout e na Mini loja (os mesmos tres caminhos que quebram dados -- com opcionais, enquanto edita outro, e recarregar-e-salvar --, com o produto de origem e o que estava em edicao comparados campo a campo); na Mini loja, a decisao medida desta aba: a copia HERDA A FOTO E A CATEGORIA, e sem a foto `mProdSalvar` recusaria a copia no primeiro clique; duplicar um OPCIONAL nas duas abas; DUPLICAR UMA FAMILIA COM O CONTEUDO DELA (um clique cria a familia e todos os pacotes com os opcionais, cada pacote com codigo proprio pela normalizacao do Pix, a familia com nome derivado, e NENHUM original tocado); APAGAR UMA FAMILIA COM CONTEUDO (a confirmacao lista os pacotes PELO NOME e nao cita os de outra familia; Cancelar nao apaga nada; confirmar apaga a familia e SO os pacotes dela; a recusa da ultima familia continua; e `aEditIdx` acompanha -- apagar pacotes que estao ANTES do que esta em edicao sem baixar o indice faria o proximo "Salvar alteracoes" gravar por cima de outro); duplicar um CUPOM nas TRES abas (a copia nasce com codigo derivado, entao a recusa de codigo repetido nao acende e o Gerar nao e barrado); duplicar MENSAGEM da Contagem (os DOIS textos ganham a marca, porque a barra mostra um ou o outro conforme a largura da tela) e OPCAO DE QUALIFICACAO (que deriva ate ficar unica, porque esta lista tem recusa de texto repetido); e as FAIXAS das listas novas (cinco botoes abaixo do item nos produtos, quatro nos opcionais, e a de cupons com dois botoes continuando na lateral). Caso 15, acrescentado em 13/09/2026: a DESCRICAO DE CADA OPCIONAL atravessa toda duplicacao -- pacote, produto nas duas abas, opcional sozinho e familia inteira. Ela entrou DENTRO de `opTupla`, a tupla que os seis comparadores "o original foi tocado?" ja usavam, e nao como uma linha nova em cada caso: um campo novo somado a mao caso a caso e exatamente como um campo novo se perde. O segundo opcional de cada cenario fica SEM descricao, de proposito -- o vazio tambem tem de atravessar. Cada caso corre num `try` proprio: numa arvore sem o botao o caso acusa e o roteiro segue. | Ao mexer em qualquer `*Duplicar`, em `aFamDel`/`aFamDuplicar`, nos `*Editar`/`*Limpar`/`*Salvar` das tres abas com catalogo, em `fcDupCod`/`fcDupNome`/`fcCtrl`, ou ao levar o duplicar a mais uma aba. A regressao NAO alcanca isto: o catalogo pode PERDER UM PACOTE INTEIRO sem que uma linha do bloco gerado mude de forma -- muda o conteudo, que e o que ninguem ve num diff de 24 mil bytes. Duas medicoes na `main`, em dias diferentes: contra a `main` que ainda NAO tinha o botao de duplicar, 11 das 23 verificacoes de entao falhavam, e duas delas eram defeitos que existiam la (codigo repetido aceito no cadastro; o aviso de pendentes mandando apertar um botao que ja nao esta na tela). Contra a `main` de 03/09/2026 (com o duplicar, sem a persistencia dos campos), 10 das 90 falham, todas no caso 3: a copia recarregada volta com o formulario em branco, e salvar e recusado com "Informe o codigo do pacote". Desde a leva 3 ele cobre tambem o unico caminho da ferramenta que APAGA varios itens de uma vez (`aFamDel` levando os pacotes junto): a medida que importa ali nao e "apagou", e sim "nenhum pacote de outra familia sumiu", e ela e feita comparando o pacote sobrevivente campo a campo com a fotografia de antes. `node scripts/verificar/duplicar-itens.mjs`. |
| `corrida-calendario.mjs` | A **CORRIDA ENTRE O `embed.js` DO TIDYCAL E O NOSSO APERTO DE MAO** (04/09/2026). O bloco tem dois mecanismos que sabem cuidar da altura do iframe, e a fonte sempre disse "nunca os dois juntos" -- mas ate esta data a intencao era violavel nos dois sentidos, e este arquivo e o que passou a medir isso. Ele nao olha o resultado (a altura final acompanha o conteudo?), que e o que `tidycal-unificado.mjs` ja faz e que e intermitente por natureza: ele olha a **corrida**, com uma sonda no `<head>` que **embrulha `window.setTimeout`** (o prazo de 2500ms e o comeco do calendario; o de 400ms so pode ter vindo de `comecarHandshake` -- e observacao direta do instante em que cada mecanismo assume, e nao deducao pelo relogio), **embrulha `window.iFrameResize`** (quando a biblioteca deles foi definida e quando o bloco a aplicou), escuta `message` (as alturas ANUNCIADAS, com o instante e o tipo de cada sinal) e amostra a altura do quadro a cada 100ms (as alturas APLICADAS). A diferenca entre as duas ultimas e o defeito inteiro. **Tres partes: 1.** a corrida **FORCADA** -- `retardar` (ver `pagina.mjs`) atrasa o `embed.js` em 8s -- e sao 8 e nao 5 porque o prazo do bloco recomeca a cada `load` do iframe, e com 5s a biblioteca ainda ganhava limpo 1 passagem em 3, entao o aperto de mao assume aos 2,5s **toda vez** e a pergunta vira "o que a biblioteca faz quando chega depois"; medido nos tres casos (aba TidyCal calibrada, aba TidyCal medindo, vitrine de pacotes), varias passagens por lado. **2.** as passagens **NATURAIS**, sem atraso, que medem a FREQUENCIA e nao o defeito. **3.** as tres degradacoes, agora com quem assumiu a vista. **Medido em 04/09/2026, `main` contra a arvore consertada:** na corrida forcada, a biblioteca era aplicada por cima do aperto de mao em **9 de 9** passagens da `main` e em **0 de 9** depois; na vitrine, a altura anunciada (1054) chegava ao elemento em **0 de 3** e passou a **3 de 3** (a `main` parava em 1021). Nas passagens naturais em modo `medindo`, com 6 cargas por lado, a altura anunciada era aplicada em **1 de 6** na `main` e em **6 de 6** depois -- e os sinais crus mostram por que: o `init` do filho chega entre 2443ms e 3212ms, quase sempre DEPOIS do prazo de 2,5s, entao o aperto de mao entrava por cima de uma biblioteca ja aplicada, o `load` do iframe devolvia o comando a ela em seguida, e os **dois** acabavam calados -- o quadro ficava nos 700px de partida com 764 anunciado. Era a intermitencia de "uma carga em quatro" registrada em 03/09/2026, e ela era esta corrida. **A parte 2 mede os DOIS enderecos**, e por uma razao medida no mesmo dia: em `tidycal.com` o calendario anuncia 764 em repouso (um `mutationObserver` depois do `init`), e no **dominio proprio** ele nao anuncia nada alem do `init:700` -- o quadro fica na altura de partida, **nas duas arvores, 3 de 3 cada**. E do endereco, e nao da arvore; medir so um faria essa diferenca virar uma conclusao errada sobre o conserto. | Ao mexer em `comecarCalendario`, `carregarBiblioteca`, `aplicarBiblioteca`, `comecarHandshake`, `pedirAltura`, `comandoDe` ou nas marcas `data-fc-lib`/`data-fc-mao`. A regressao NAO alcanca isto: quem escreve no elemento nao aparece no texto gerado, e o defeito era mudo -- o calendario simplesmente ficava do tamanho errado. **PRECISA DE INTERNET**, e quando falhar por rede a leitura certa e "nao mediu". `node scripts/verificar/corrida-calendario.mjs`, ou com outra referencia como argumento; `FC_FORCADAS=n` e `FC_PASSAGENS=n` mudam quantas passagens de cada tipo, e `FC_PARTES=2` roda so uma das tres partes (existe para reinvestigar um achado sem repetir a rodada inteira). **NAO RODE junto com outro arquivo que fale com o TidyCal.** |
| `limites-visiveis.mjs` | OS **LIMITES VISIVEIS NOS CAMPOS QUE ALIMENTAM API** (11/09/2026). A rodada nao podia mudar um byte de saida -- entao a regressao, sozinha, so sabe dizer que nada mudou. O que ela nao alcanca e o que a rodada entregou: o **numero na tela** que diz quanto do campo chega ao payload. Contador errado nao quebra nada; ele mente, e o defeito aparece no aplicativo do banco de quem paga. **Cinco partes. 1.** o contador conta o que SOBRA (decisao D2): digita nome com acento e com `&`, cidade com acento e identificador com hifen e barra, LE o numero da tela e compara com o que o gerador **realmente emitiu** -- o BR Code extraido do link de cobranca, lido por um analisador de TLV escrito DENTRO do proprio arquivo, porque o `lerTlv` do projeto nao pode ser ao mesmo tempo a maquina que monta e a testemunha que confere. Cobre tambem o **segundo teto** da descricao da cobranca (200 no link, 127 no PayPal), que aparece e some na hora certa. **2.** o CURSOR NO MEIO (decisao D4): poe o cursor no meio do texto, digita um caractere recusado e um aceito, e confere a posicao depois de cada um -- filtro ingenuo passa no teste que digita do inicio ao fim e falha aqui, e e assim que ele chega em producao. **3.** o `maxlength` e o COLAR: digitar alem do teto para no teto, colar alem do teto deixa o texto cortado no teto, o aviso de corte aparece com **os dois numeros** e some quando o operador mexe no campo de novo. **4.** a correcao a vista de `change`/`blur` (`pTxidAjustar`) continua consertando o valor que chega SEM tecla nenhuma -- estado restaurado, backup importado, preset aplicado. **5.** o PIX VALIDO NO LIMITE: nome com 25, cidade com 15 e identificador com 25 depois da limpeza, com os campos 59, 60 e 62>05 conferidos um a um e o **CRC16 recalculado por uma implementacao independente** deste arquivo. Fecha cobrando zero erro de console na passagem inteira. | Ao mexer em `FC_LIM_CAMPOS`, `fcLimIniciar`/`fcLimFiltrar`/`fcLimAtualizar`, em `pTxidLimpo`, `fcPixTexto`/`semAcento`, `pixLimpar`, `FC_LIM_PP`, ou ao levar o contador a um campo novo. A regressao NAO alcanca isto -- por definicao: esta rodada existe para NAO mudar saida nenhuma. Nao precisa de internet. `node scripts/verificar/limites-visiveis.mjs`. |
| `id-orcamento.mjs` | O **ORCAMENTO DO IDENTIFICADOR DE CONCILIACAO** (11/09/2026), com os blocos RODANDO e com o lado **ANTES** medido de verdade. Em duas abas o identificador que chega ao Pix e COMPOSTO -- o que o operador digita mais uma cauda que o bloco acrescenta --, e o estouro nao dava erro em lugar nenhum. Ele gera os blocos da referencia (`94042b6` por padrao -- o commit ANTERIOR ao conserto, via `git archive`, o mesmo caminho de `regressao.sh`; a referencia **nao pode ser `main`**, que desde 11/09/2026 ja carrega a recusa e faria o lado "antes" medir a si mesmo) e os da arvore de trabalho, e mede os dois lados. **Seis partes. 1.** a **COLISAO** na referencia: dois pacotes diferentes (`MINIENSAIO1H` e `MINIENSAIO4H`, prefixo `FOTOCERTAESTUDIO`) chegam ao extrato com o MESMO txid -- medido, `FOTOCERTAESTUDIOMINIENSAI` nos dois. **2.** a mesma configuracao **RECUSADA** na arvore de trabalho, sem gerar bloco nenhum. **3.** a **TRUNCAGEM** da Mini loja na referencia: o cliente le `LOJAFOTOCERTAVITORIA-MYPV1GG0` e o extrato recebe `LOJAFOTOCERTAVITORIAMYPV1` -- e o txid e um PREFIXO do que ele leu, ou seja foi cortado, nao trocado. **4.** a mesma configuracao recusada. **5.** o **NEGATIVO e a FRONTEIRA**, medidos um caractere por vez pela INTERFACE (a ferramenta roda dentro de uma IIFE, e espiar variavel de la seria medir a intencao em vez do comportamento): a recusa comeca exatamente um caractere depois do ultimo aceito, e o numero que a mensagem DIZ e o mesmo que a ferramenta FAZ. Inclui o caso que uma recusa desastrada barraria: hifen e sublinhado **nao** gastam orcamento, porque somem na limpeza do txid (20 digitados, 17 uteis, aceito). **6.** o **LIMITE EXATO**: no teto o txid usa os 25 caracteres inteiros, o payload e lido por um TLV e um CRC16 **escritos dentro do arquivo** -- conferir o projeto com o `lerTlv` do projeto seria perguntar a ele se concorda consigo mesmo --, e na Mini loja o que o cliente le e, caractere por caractere, o que chega ao extrato. Medido em 11/09/2026: limites de **17** (loja) e **13** (agendamento), com caudas de 8 e 12. | Ao mexer em `novoPedido`, `idConciliacao`, `diaHoraId`, `sufAleatorio`, `limpaId`, nas recusas de `mRecusa`/`aRecusa` que citam o orcamento, ou em qualquer coisa que mude a FORMA do identificador. A regressao NAO alcanca isto: a recusa existe justamente para configuracao que a regressao nunca exercita (o cenario dela usa codigos curtos), e o defeito que ela conserta e mudo por natureza -- colisao de identificador nao da erro em lugar nenhum, ela aparece como dois pagamentos que ninguem consegue distinguir. Nao precisa de internet. `node scripts/verificar/id-orcamento.mjs`, ou com outra referencia como argumento. |
| `achar-textos.mjs` | **ACHAR UM TEXTO** (11/09/2026): a busca da ferramenta e o eco do texto atual em cada rotulo. A rodada nasceu de o dono querer mudar o texto `Sem opcional` que o cliente le, procurar na ferramenta, **nao achar**, e pedir que o texto virasse configuravel -- sendo que ele ja era (`u-txt-semopcional`) e funcionava. Defeito de DESCOBERTA nao aparece em saida de gerador nenhuma, entao a regressao nao alcanca nada disto. **Sete provas. 1.** o **CASO DO DONO, ponta a ponta**: da aba Slideshow, com a secao de textos do Checkout recolhida (`display:none`), procurar "Sem opcional" e chegar ao campo **visivel, destacado, com foco e acima da barra grudada no topo** -- e com o `aria-expanded` e a palavra da seta acompanhando. **2.** a **NEGATIVA FALSA, provada que nao acontece**: a busca varre os CAMPOS DA TELA e nao as tabelas -- uma busca apoiada nas tabelas diria "nao encontrado" sobre coisa que existe. Oito campos sao procurados, um por aba (`u-t9`, `b-selo-txt`, `l-m3`, `p-t5`, `c-ctatxt`, `m-t11`, `a-ob-fb-quando`, `t-ob-fb-tipo`): os seis primeiros entraram nas tabelas na leva 2 e os dois ultimos em 11/09/2026 (os nove textos reserva dos marcadores -- ver `textos-reserva.mjs`), e os oito tem de ser achados do mesmo jeito, antes e depois: e isso que prova que a busca nunca dependeu da tabela. Mais a conferencia de que o **codigo gerado** (readonly) fica de fora. **3.** **acento e caixa**: `sem opcional`, `SEM OPCIONAL` e `Sem Opcional` dao o mesmo conjunto; busca sem acento acha valor com acento; e a negativa verdadeira e dita como negativa. **4.** a **busca nao altera nada**: o estado gravado antes e depois e identico caractere por caractere (com aquecimento por todas as abas antes de fotografar, senao a conta das correcoes de faixa seria atribuida a busca). **5.** o **eco mostra o VALOR ATUAL**: mudar o texto muda o rotulo, e o qualificador que ele ja tinha continua la, num `<small>` so. **6.** a **forma do eco**: frase longa cortada em palavra inteira com reticencia, e rotulo que ja diz o texto inteiro nao o repete. **7.** os campos que **nao tinham rotulo proprio** (`c-txt-suf-h/m/s` e `c-rh/rm/rs`) agora tem. Mede tambem, e imprime, quantos campos de texto existem nas abas (o numero sai da varredura, nao de uma frase escrita a mao). | Ao mexer na busca (`fcs*`), no eco dos rotulos, em `fcdLigar`/`fcdEstado`/`fcdAbrir`, em `fcTxtTabelas` ou em qualquer rotulo de campo de texto. Nao precisa de internet. `node scripts/verificar/achar-textos.mjs`. |
| `textos-migrados.mjs` | **OS TEXTOS QUE ENTRARAM NAS TABELAS** (leva 2, 11/09/2026): os 58 campos de texto que o cliente le e que viviam FORA das `*_TXT_DEFS` passaram para dentro delas. E refatoracao -- `regressao.sh` prova que nenhuma saida mudou um byte --, e este arquivo prova os tres defeitos que a regressao NAO alcanca. **Seis provas. 1.** a **tabela alcanca** cada um deles, com a chave que o estado ja gravava; e **nao alcanca** o que esta declarado fora (hoje so `m-cod`, que e identificador com regex e teto) -- exclusao declarada, para que o dia em que alguem o migrar este teste fale. Foi exatamente o que aconteceu com os nove textos reserva dos marcadores: eles eram a exclusao declarada mais pesada (mudavam o **formato do que fica gravado**), o dono autorizou, eles migraram em 11/09/2026 e passaram para a lista dos migrados -- com prova propria da migracao em `textos-reserva.mjs`. Como `T_TXT_DEFS` e `A_TXT_DEFS` deixaram de terminar em `];` (elas fecham com `.concat(fcObFbDefs(X_OB_VARS,'p'))`), o leitor de tabelas deste arquivo **expande o concat** lendo `X_OB_VARS` -- sem isso as duas seriam lidas como incompletas e a prova 1 acusaria campo migrado como ausente. **2.** o **eco no rotulo** mostra o texto atual de cada um. **3.** a **fabrica divergente** e denunciada: uma copia da arvore com o `value=` de TODOS eles alterado e servida, e a barra vermelha tem de acender nomeando o passo dos textos e cada campo. **4.** **"Restaurar padroes" repoe de verdade** nos quatro botoes que existem (`s-limparbtn`, `l-padroes`, `c-limpar`, `p-limpar`) -- e as abas sem botao nenhum sao MEDIDAS, nao supostas. **5.** o **formato gravado** e identico ao da referencia (`main` por padrao), chave por chave: as duas arvores servidas em portas separadas, preenchidas do mesmo jeito, o `fcConstrutores` comparado canonicamente -- mais a conferencia de que nenhuma chave do estado virou objeto. As **nove chaves novas** de 11/09/2026 sao a unica diferenca esperada, e saem da comparacao **depois** de serem cobradas uma a uma (existem aqui com o texto do seu campo, e nao existem na referencia); a chave antiga `obfb` continua nos dois lados e continua sendo exigida identica. **Toda rodada seguinte que mexe no formato gravado entra na mesma disciplina** -- chave nova, fabrica trocada, chave RENOMEADA (valor exigido igual dos dois lados) e, desde a leva 7 (14/09/2026), **chave nova dentro de `form`**: o caso em que o campo novo faz crescer o INTERIOR de uma chave que ja existia nos dois lados. Sem essa quarta lista a comparacao final so saberia dizer "a aba u diverge" -- verdade, inutil, e do tipo que se aprende a ignorar. **6.** a **PROVA DA APOSTROFA**, que e a razao de o arquivo existir: na Captacao de leads o `escJs` acontecia ao LER o campo e desceu para as linhas de emissao, e com os valores de FABRICA a saida e identica com ou sem ele -- **a regressao byte a byte nao denuncia o esquecimento**. Os 58 recebem um valor com apostrofa, aspas, barra invertida e `</script`, os blocos sao gerados e EXECUTADOS numa pagina, e o texto tem de chegar inteiro: a tela, a ficha do produto da Mini loja e a mensagem do WhatsApp (colhida interceptando o `window.open` do proprio bloco -- ela nunca aparece na tela). A pergunta de qualificacao e um opcional na Mini loja entram no cenario deste teste de proposito: sem eles, `l-qlabel`/`l-qdica`/`l-qmsg` e `m-t2` ficariam fora de toda medicao de execucao. | Ao mexer em `fcTxtLer`/`fcTxtRestaura`/`fcTxtFabricaDiverge`/`fcTxtTabelas`, em qualquer `*_TXT_DEFS`, nos `coleta`/`restaura`/`cfg` das abas, ou nos botoes de reposicao. Nao precisa de internet. `node scripts/verificar/textos-migrados.mjs [referencia]`. |
| `textos-reserva.mjs` | **OS NOVE TEXTOS RESERVA DOS MARCADORES** (11/09/2026): os textos que a pagina de obrigado mostra quando o TidyCal **nao manda** o dado -- no lugar do nome, da data, do horario. Cinco na aba Agendamento TidyCal, quatro na Agendamento por pacote. Eram os ultimos campos fora das tabelas de texto, e ficavam fora por um motivo so: os nove dividiam **uma** chave de estado (`obfb`), com os valores colados por um caractere de controle. Cada campo passou a ter a propria chave -- isso **muda o formato do que fica gravado**, e a migracao e a razao de o dono ter podido autorizar. **Oito fases. 1 e 2.** o **backup antigo**: o estado antigo nao e escrito a mao -- e colhido da **propria arvore de referencia** (`main`), servida numa porta separada, em quatro cenarios (textos distintos, todos vazios, texto hostil, e texto **com o caractere de controle dentro**); o mesmo JSON e semeado na arvore de trabalho e se compara **campo por campo**, mais a exigencia explicita de que cada um dos nove chegou ao **seu** campo -- e nao que 'algo' chegou. Mais o estado **sem `obfb` nenhum** (anterior ao proprio recurso: cai na fabrica) e o `obfb` **mais curto** que a tabela. As duas abas tratavam VAZIO de forma diferente, e isso e medido e dito por extenso: a TidyCal aceitava vazio gravado, a Agendamento por pacote repunha a fabrica. **3.** o **preset antigo**, salvo pela referencia e aplicado aqui pelo botao Aplicar da propria lista, depois de os nove serem sujados -- sem sujar, 'o preset trouxe o texto' seria indistinguivel de 'o campo nunca mudou'. **4.** o **formato novo**: uma chave por campo, a chave antiga gravada como **projecao** delas na ordem certa, e a ida e volta por recarga. **5.** o **eco no rotulo** e a **ordem do endereco** gerado, com os cinco marcadores ligados **ao contrario** e o endereco comparado caractere por caractere com o de `main`. **6.** a **fabrica divergente** plantada nos nove e denunciada. **7.** o **caractere de controle**, MEDIDO pelos quatro caminhos de entrada (teclado, insercao de texto, preenchimento e **colagem de verdade**, com a area de transferencia da plataforma): ele chega intacto, logo o formato antigo **e** ambiguo -- o que se cobra e que a arvore de trabalho se comporte, diante de um estado assim, **exatamente como a referencia** (a ambiguidade e herdada, nao criada), e que o formato novo acabe com ela. **8.** os nove com **texto hostil** (apostrofa, aspas, barra invertida e `</script`) e o bloco **executando** numa pagina **sem os parametros no endereco**, que e a unica circunstancia em que o texto reserva aparece: `t-out5` e `a-out3`, com os marcadores escritos no corpo. Foi esta fase que achou o `escJs` no lugar de `escJsD` no unico literal de aspas duplas da ferramenta (`a-out3`), defeito anterior a esta rodada. | Ao mexer em `fcObFbChave`/`fcObFbDefs`/`fcObFbJuntar`/`fcObFbMigrar`, em `T_OB_VARS`/`A_OB_VARS`, nos `coleta`/`restaura` das abas Agendamento TidyCal e Agendamento por pacote, ou em qualquer coisa que toque o **formato do que fica gravado**. Nao precisa de internet. `node scripts/verificar/textos-reserva.mjs [referencia]`. **A referencia NAO pode ser `main`** (padrao: `77d9db2`, o commit anterior a rodada): desde 11/09/2026 a `main` ja grava no formato NOVO, e o lado "antes" passaria a medir a si mesmo -- medido em 12/09/2026, sete falhas que nao eram defeito nenhum. Mesma armadilha de `id-orcamento.mjs`. |
| `meio-prioritario.mjs` | **A ORDEM DOS MEIOS DE PAGAMENTO, CONFERIDA NO DOM** (12/09/2026). Ate esta rodada **nao havia prova nenhuma** de que o Pix aparece antes do PayPal, ou depois: a ordem era efeito de concatenacao de string em quatro geradores, e um `grep` por `fcu-sep|fcm-sep|fcpg-sep|pixlinha|fcu-botoes` em `scripts/` devolvia zero -- quer dizer, a proxima rodada podia desfazer esta em silencio, e a regressao byte a byte nao acusaria nada (ela compara a arvore com a REFERENCIA; se as duas mudassem juntas, nao ha o que divergir). **Cinco provas, nas QUATRO abas de pagamento e nas DUAS escolhas do campo "meio prioritario". 1.** a **ORDEM pelo INDICE DOS FILHOS** no pai comum -- e a pergunta literal "quem e o filho de numero menor", e nao "quem aparece antes no texto do bloco", que responderia outra coisa. **2.** o **SEPARADOR fica ENTRE os dois**, sempre, medido contra os dois marcadores. **3.** os **DOIS NUMEROS a vista**, e o que esta em destaque e o do meio prioritario -- destaque medido por `getComputedStyle().fontSize` no Checkout e na Mini loja (onde os dois numeros sao linhas do carrinho) e pelo proprio valor na Agendamento por pacote e no Link de cobranca. **4.** o **valor que o PayPal COBRA e o CHEIO**, nas duas escolhas: `createOrder` e o **do bloco**, chamado com um `actions.order.create` que so devolve o objeto -- e este e o defeito que a rodada podia criar, preco do Pix grande na tela e cobranca cheia no cartao sem aviso. **5.** as **FRASES** que apontam para o outro meio leem certo nas duas ordens: nenhum "ou pague com Pix" antes do Pix, nenhum "Use o Pix acima" apontando para baixo (as duas de erro sao lidas no TEXTO do bloco entregue -- elas so aparecem na tela quando o SDK do cartao falha, e forcar essa falha oito vezes custaria mais do que vale). O SDK do PayPal e interceptado pelo mesmo caminho da sonda da previa da ferramenta (`fcPvSondaPP`). **EM 13/09/2026 A PERGUNTA DO SEPARADOR TROCOU** (decisao 24 do dono): ele passou a ser **neutro** (`OU`) nas quatro abas, e frase neutra nao aponta para lado nenhum. A assercao antiga ("o separador nomeia o meio de BAIXO") virou quatro vermelhos por dia sem defeito por tras, e foi substituida pela propriedade que nao envelhece: o separador de fabrica **nao nomeia nem o Pix nem o cartao**, nas duas ordens -- um separador que nao nomeia meio nenhum nao tem como apontar para o lado errado. O que **continua** apontando (a linha 2 do Agendamento por pacote) continua medido. | Ao mexer em qualquer coisa que decida a ORDEM ou o DESTAQUE nos quatro geradores de pagamento (`uBloco`/`mBlocoRuntime`/`mBloco`, `montarPreco` e `secaoCartao`/`pix` da aba Agendamento por pacote, `pJs`), no campo `*-prio`, ou em `FC_ORD_PARES`. Nao precisa de internet. `node scripts/verificar/meio-prioritario.mjs`. |
| `meio-prio-migracao.mjs` | **A MIGRACAO DO MEIO PRIORITARIO, E O OUTRO LADO DA REGRESSAO** (12/09/2026). A rodada trocou o **padrao de fabrica** de duas frases do Checkout e da Mini loja (`ou pague com Pix` -> `ou pague com cartao`, porque o Pix passou a vir em cima). Trocar o padrao **nao chega** a quem ja usou a ferramenta: os campos de texto sao gravados a cada tecla, entao o estado do dono ja contem a frase antiga, e valor gravado vence padrao -- sem migracao o bloco dele sairia dizendo "ou pague com Pix" logo ACIMA do Pix. **Cinco partes. 1 e 2.** o estado e **colhido da propria referencia** (`git archive` + a ferramenta de la gravando de verdade), nunca escrito a mao aqui: valor igual a fabrica antiga **migra**; valor que o dono escreveu fica **intocado** -- e o campo ao lado, que estava na fabrica, migra do mesmo jeito na mesma passagem. **3.** a **barra vermelha** da fabrica divergente (`#fc-falhas`, o unico efeito visivel de `fcTxtFabricaDiverge`, que vive dentro da IIFE e nao e alcancavel de fora) **nao acende** numa ferramenta recem-aberta, nem depois de gravar cada uma das duas escolhas e recarregar. **4.** o **OUTRO LADO da regressao**: com o meio prioritario em "cartao", o Checkout e a Mini loja voltam a ser **byte a byte identicos** aos da referencia, e com "Pix" a vitrine da Agendamento por pacote tambem -- mais a prova do contrario, de que a outra escolha realmente muda as saidas (senao tudo isso seria vacuo). **5.** **com um meio so**, a escolha nao muda um byte: a ferramenta desliga o campo (`fcOrdSo1`) mas o valor gravado continua indo ao gerador, de proposito -- forcar o padrao em `uCfg`/`mCfg` esconderia um defeito em vez de impedi-lo, e esta e a prova de que nao ha o que esconder. | Ao mexer em `fcOrdMigrar`/`fcOrdVirar`/`fcOrdRestaurar`/`fcOrdSo1`, em `FC_ORD_PARES`/`FC_ORD_TXT`/`FC_ORD_FABRICA`, nos `coleta`/`restaura` das quatro abas de pagamento, ou em qualquer coisa que mude o **formato do que fica gravado**. Nao precisa de internet. **As partes 1, 2 e 4 so MEDEM contra uma referencia ANTERIOR a 12/09/2026**; desde que a rodada chegou a `main`, a referencia padrao deixou de servir para elas, e ate 13/09/2026 este arquivo acusava **nove falhas todo dia sem nenhum defeito por tras** (medido: as mesmas 9 de 52 numa arvore limpa de `main`). Vermelho que e sempre vermelho esconde o proximo, que seria de verdade -- entao ele passou a **detectar** a situacao e dizer "NAO MEDIU" nas partes 1 e 2, e a parte 4 **troca de pergunta**: contra uma referencia que ja tem a rodada, o que se cobra e que a escolha de FABRICA (Pix) saia byte a byte identica a dela e que a outra escolha realmente mude. Para medir a MIGRACAO de verdade: `node scripts/verificar/meio-prio-migracao.mjs 4c66719` (a ultima antes da rodada) -- medido em 13/09/2026, 51 verificacoes, tudo OK. |
| `sinal.mjs` | **A COBRANCA DE SINAL, COM OS BLOCOS RODANDO** (13/09/2026). Ate esta data o caminho do sinal tinha **zero** verificacao em todo o arnes -- um `grep` por `u-sinal|m-sinal|sinalAgora|saldoDepois|sinalRecusa|SINAL_TIPO|SINAL_VALOR` em `scripts/` devolvia duas ocorrencias, as duas em COMENTARIO. Como `u-sinal`/`m-sinal` nascem em "nao" e nada as ligava, as ~40 linhas condicionais de `FC_CARRINHO_SRC.sinal`, `fcTotalPixSrc` (ramo do meio), `fcPpBotoesSrc`, `uBloco` e `mBloco*` saiam **zero vezes** da regressao byte a byte: quem mexesse na conta do sinal mexia sem rede. **Seis provas, nas duas abas que tem sinal e em tres configuracoes (percentual de 50%, valor fixo de R$ 100,00 e percentual de 1%). 1.** a **CONTA** nos dois tipos, sobre o total **ja com cupom** -- a ordem declarada e quantidade multiplica, cupom incide, sinal sai do total com cupom. **2.** o **numero que o cliente le e o que as duas pontas cobram**: o **campo 54** do payload Pix, relido por um leitor TLV **escrito dentro do teste** (regra da casa: nunca conferir o projeto com uma funcao do projeto -- o CRC16 tambem e recalculado ali, do polinomio), e o `amount.value` do `createOrder` lido do proprio bloco pela sonda do SDK; os dois iguais, centavo por centavo, a linha da tela. **3.** o **ARREDONDAMENTO**, com os casos escolhidos por criterio e nao por sorteio: sao os que caem na familia do **meio centavo** -- aquela em que `Math.round` e `toFixed(2)` discordam --, que e o defeito de ago/2026 (60.097 divergencias em 2.002.000 combinacoes). A varredura que escolheu o catalogo mediu 39.006.394 combinacoes, 767.882 com o sinal cru em meio centavo exato e 313.708 (0,80%) em que os dois metodos devolvem centavos diferentes, **sempre com o `Math.round` por cima**; o catalogo e o percentual sao os que poem 8 dos 15 subconjuntos do carrinho nessa familia, e quatro deles entram na medicao. **4.** as **TRES RECUSAS** de `sinalRecusa()` e os **QUATRO** consumidores que leem dela -- linha vermelha do carrinho, clique do PayPal, botao do Pix e resumo copiavel --, recusando **juntos**, com a mesma frase (o resumo era o unico que nao respeitava a recusa, e foi corrigido uma vez). **5.** o **desconto do Pix zerado**: com sinal ligado, `totalPix()` e `sinalAgora()`, a linha do Pix nao e emitida nem no texto nem no DOM, e nao existe segunda forma de `totalPix` no bloco. **6.** as **tres linhas na tela** (total, sinal, saldo) e as **tres do WhatsApp**, lidas da URL que o proprio bloco passou ao `window.open`. Fecha com as dividas **D2** (o campo do desconto do Pix da Mini loja passou a ser desabilitado com sinal ligado, como o do Checkout) e **D3** (o aviso passou a seguir so o sinal), medidas na interface por **visibilidade real da caixa** e nao por `getComputedStyle` -- o computed `display` de um elemento dentro de um ancestral `display:none` continua sendo o dele proprio, e mediria errado exatamente no caso em que quem esconde e o pai. **A ABA AGENDAMENTO POR PACOTE entrou em 13/09/2026 (rodada C)**, numa secao propria no fim do arquivo, com as seis provas acima em **oito** passagens pela ferramenta (percentual de 50% e de 1%, valor fixo de R$ 100,00, os dois meios prioritarios, as duas SEM sinal que servem de lado "antes" e duas com **texto hostil**) e **mais tres que so existem la**. **7.** o **IDENTIFICADOR DE CONCILIACAO IMUNE AO SINAL**: ele e calculado so a partir do pacote e do horario, antes de o carrinho existir, e essa independencia e a correcao do defeito mais caro daquela rodada (um extrato com varias cobrancas diferentes para uma reserva so); aqui ele e lido de onde o cliente de fato o manda -- o `custom_id` do pedido ao SDK do cartao, nunca uma variavel interna (o `sku` deixou de carrega-lo na leva 7, 14/09/2026: ele passou a ser o SKU **daquele item**, e a prova cobra que sem SKU cadastrado a linha saia **sem o campo**) -- e conferido nas **quatro** combinacoes de (meio prioritario x sinal), depois de **recarregar** a pagina e depois de **mexer no carrinho**, com uma prova a mais de que o dinheiro de fato mudou naquela passagem (senao a anterior nao teria medido nada). **8.** o **ENCONTRO COM O MEIO PRIORITARIO**: com sinal o desconto do Pix e zerado na origem, entao "preco do Pix" e "preco cheio" viram o mesmo numero -- e esta aba tem duas pecas que as irmas nao tem, a **segunda linha** (`ou {valor} no cartao`) e o **selo** `-{pct}% no Pix`. Medido no DOM: sem sinal existem **dois numeros diferentes** e **um** selo; com sinal, **nenhuma** segunda linha e **nenhum** selo. **9.** o **PARCELAMENTO SOBRE O SINAL** (decisao do dono): a parcela lida na tela e `sinalAgora()/PARCELAS`, e a prova cobra tambem que ela **nao** e `total()/PARCELAS`. **10. O BOTAO "JA PAGUEI" (13/09/2026).** Ate aquele dia esta aba mostrava o Pix ao cliente e **nao tinha** o botao que as tres irmas tem dentro da area do Pix: o cliente copiava o codigo, pagava no banco e nao tinha como avisar. Nao era decisao -- era omissao. A secao mede a **posicao no DOM** (dentro da area do Pix, logo depois do aviso de que o Pix nao confirma sozinho) e a consequencia dela: **antes** de "Gerar Pix" o botao nao esta visivel, **depois** esta, e mexer no carrinho esconde a area e o botao junto. Mede a **mensagem** lida da URL do `wa.me` que a propria ancora carrega, nos **dois ramos** -- com sinal (abertura de sinal + as tres linhas total/sinal/saldo) e sem sinal (abertura inteira + desconto do Pix + linha unica do valor pago) --, **com e sem cupom** e com opcional marcado, sempre conferindo contra o numero que esta **na tela**. Mede o **identificador em tres lugares na mesma passagem** -- a mensagem, o campo **62/05** do payload Pix (lido pelo mesmo leitor TLV independente, que passou a extrair o txid) e o `custom_id` do pedido ao cartao --, e que ele **nao muda** quando o dinheiro muda (com a prova do outro lado: o dinheiro mudou mesmo). E mede o estado **SEM WhatsApp**, que so existe editando o bloco publicado a mao (a aba **recusa** gerar sem WhatsApp): nenhum elemento carrega a classe, nenhuma ancora sobra na area do Pix e o Pix continua sendo gerado. Mais **a ausencia declarada** que continua valendo: esta aba tem **tres** consumidores de `sinalRecusa()` e nao quatro (nao ha resumo copiavel, e por isso `txtSinalRecusado` nao nasce aqui), `botaoZap` continua **sem saber de dinheiro** (recebe a frase pronta; quem a monta e `zapMsgPago`, num lugar so) e a URL do WhatsApp e montada **num lugar so** (`zapHref`). E mais o **texto hostil** nos quatro campos do sinal e nos **sete do "Ja paguei"** (apostrofa, barra invertida, aspas duplas, acento e `</script`), com o bloco **executando** e pelos **dois caminhos de escape** -- `escJsD` direto (rotulo do botao, abertura) e `aTplJs`, que parte o texto no marcador e escapa cada pedaco (linha do identificador, do cupom, do saldo, do valor): como esses campos ainda nao estao na tabela `TEXTOS` de `cenario.mjs`, este e o unico lugar que mede o escape deles -- e a medida e o documento inteiro sobreviver, porque um `</script` nao blindado corta o `<script>` do bloco no meio. | Ao mexer em `FC_CARRINHO_SRC.sinal`, em `fcTotalPixSrc`, no `usaSinal` de `fcPpBotoesSrc`, nos campos `*-sinal*` das tres abas, no identificador de conciliacao da Agendamento por pacote, ou ao levar o sinal para uma aba nova. Nao precisa de internet. `node scripts/verificar/sinal.mjs`. |
| `sinal-cobranca.mjs` | **O SINAL NO LINK DE COBRANCA, E A TRAVESSIA DE VERSOES** (13/09/2026, rodada D). E a unica suite do arnes que mede tambem o que ja SAIU: existem links no WhatsApp de clientes e um bloco `/pagar` colado no site que **nao e versionado**. O sinal entra no SELO (parametro `n`, o TOTAL da cobranca -- o sinal ja mora dentro do codigo Pix, no campo 54), e selo e contagem: acrescentar um item a conta de TODO link derrubaria de uma vez todas as cobrancas ja enviadas. **Sete partes, 319 verificacoes.** **1. A PROVA QUE MANDA:** os oito formatos de link gerados pela REFERENCIA (`git archive`, como as outras suites) -- com e sem `t`/`x`, com e sem PayPal, com e sem prazo, com e sem identificador, e o de acentos -- rodados no bloco NOVO **e** no bloco da referencia, exigindo o **mesmo valor na tela**, o mesmo rotulo, a mesma linha do Pix e o mesmo campo 54; se esta falhar, a rodada nao entrega. **2. O caminho inverso, declarado:** link COM sinal e **recusado** pelo bloco da referencia -- e isso e o comportamento correto, medido em vez de suposto, porque e dele que sai o aviso ao dono ("regere e recole o codigo 1, uma vez so"). **3. As adulteracoes:** as oito de sempre sobre um link com desconto, mais oito sobre um link com sinal (`n` trocado, `n` apagado, `pp` acrescentado, ...), mais a nona desta rodada -- `n` junto de `t`/`x` --, mais quatro **FORJADOS** (total zero, total menor que o sinal, total ilegivel, total com tres casas) selados pela conta do selo **transcrita dentro do teste**: sem eles as guardas do bloco seriam inalcancaveis. O selo nao e segredo, e por isso forjar e legitimo aqui. **4. As seis provas do sinal** nas oito cobrancas do catalogo: a conta, o **campo 54 relido por leitor TLV escrito no proprio teste** (com o CRC16 recalculado do polinomio), o `amount.value` do `createOrder` lido do proprio bloco por uma **sonda sem rede** (um `window.paypal` falso instalado antes do bloco, que faz o `appendChild` do SDK disparar o `onload` sem baixar nada -- nenhum Client ID sai daqui), os **quatro casos do meio centavo** (varredura de 39.203.197 combinacoes: 774.110 em meio centavo exato e 317.363 (0,810%) com `Math.round` e `toFixed(2)` discordando, **sempre** com o `Math.round` por cima), as tres linhas da tela conferidas **pela ordem no DOM** e a linha do WhatsApp lida do `href` que o proprio bloco montou. **5. As recusas da GERACAO** -- e o **pedido em zero** da rodada F, que **aqui nao existe** e a suite diz por que: nao ha carrinho, o valor e digitado e a geracao o recusa em zero, entao o equivalente sao as cinco recusas nomeadas, que acontecem **antes** de o link existir. Mais a divida **D2** nesta aba (campo do desconto do Pix desabilitado, aviso a vista e o numero preservado). **6. A `/cobrar` gera o MESMO link, caractere por caractere** -- ela e o TERCEIRO gerador do mesmo endereco, e um caractere de diferenca e o selo nao fechando com o cliente na frente. **7. A maquinaria entra SEMPRE no bloco**, com ou sem sinal na cobranca aberta (mesma regra do desconto), e **nenhum uso de `totalPix()` alcanca o caminho do sinal** -- medido pela posicao no texto, nao por uma palavra na linha. Tem um **PORTEIRO** no inicio: arvore que nao gera bloco nem link **para** a suite, porque bloco vazio nao desenha nada e "nao desenhou" passaria por "nao recusou". Isso aconteceu de verdade nesta rodada -- a guarda de versao do `index.html` parou a ferramenta inteira, em silencio, e 30 verificacoes ficaram verdes sem medir nada. | Ao mexer em `seloDe`/`pSeloDeSrc`, em `pBusca`/`pLinkDe`, em `pValorPix`/`pDescPct`, nos campos `p-sinal*`, no `pJs` da aba Link de cobranca, ou **sempre que um parametro novo entrar no link**. Nao precisa de internet. A referencia e o que faz o papel de "a versao de hoje": `node scripts/verificar/sinal-cobranca.mjs [referencia]` (padrao `main`). Contra uma referencia que JA tenha o sinal ele avisa que as partes 1 e 2 deixam de significar travessia. |
| `zap-saldo-migracao.mjs` | **A MIGRACAO DA LINHA DO SALDO NO WHATSAPP** (13/09/2026, divida D1). Ate esta rodada a linha do saldo na mensagem do WhatsApp **nao era campo**: o gerador montava `TXT_SALDO + ': *{valor}*'`, e `TXT_SALDO` e o rotulo da **TELA** (`t9`). Dar campo proprio a uma frase **derivada de outra** quebra quem personalizou a origem, e quebra em **silencio**: quem trocou o rotulo da tela para "Falta pagar" via a mensagem dizer `Falta pagar: *R$ 200*`, e passaria a ver `Restante na entrega` -- o padrao de fabrica do campo novo -- sem nada na tela avisando. **A regressao byte a byte nao pega isso**: com os textos de fabrica as duas formas produzem o mesmo texto, e foi exatamente assim que a divergencia da rodada (uma linha em `m-out`) passou como inofensiva. **Tres partes. 1.** rotulo da tela personalizado na referencia -> a linha do WhatsApp **nasce dele**, e NAO cai no padrao de fabrica. **2.** rotulo na fabrica -> a linha fica na fabrica dela, e a **barra vermelha** (`#fc-falhas`) nao acende. **3.** estado que **ja tem** a chave nova e escolha do dono: a migracao nao o toca. O estado e **colhido da propria referencia** (`git archive` + a ferramenta de la gravando de verdade), nunca escrito a mao. | Ao mexer em `fcZapSaldoMigrar`, em `zapSaldo`/`txtZapSaldo`, ou **sempre que uma frase derivada de outra ganhar campo proprio** -- e o molde desse risco. Nao precisa de internet. **So MEDE contra uma referencia ANTERIOR a 13/09/2026**; com uma que ja tenha o campo ele diz "NAO MEDIU" em vez de fingir verde. `node scripts/verificar/zap-saldo-migracao.mjs 4c66719`. |
| `descricao-opcionais.mjs` | **A DESCRICAO DE CADA ITEM OPCIONAL** (13/09/2026). O dono pediu poder cadastrar uma descricao por opcional, "sem destaque, como na descricao curta do produto", e tomou **duas decisoes** que este arquivo existe para travar: ela aparece **so na hora de escolher** (no **carrinho** continuam so nome e valor), e texto longo **quebra em varias linhas**, nunca corta. **Seis partes, nas TRES abas que tem opcionais** (Checkout, Mini loja, Agendamento por pacote). **0.** com o campo VAZIO nem a regra de CSS nem o JS que a desenha saem do bloco -- a mesma pergunta que a regressao responde contra `main`, feita de um jeito que continua valendo depois do merge. **1.** com o bloco **RODANDO a 375px** (a largura do aparelho do dono): sem descricao, ZERO elementos; com descricao, ela vem **depois na ordem do DOM** e **abaixo na tela**, recuada 26px (debaixo do nome, nao do marcador), em **#777 e 12px**; e so no opcional que tem -- o outro nao inventa. **2.** **A DESCRICAO NAO ENTRA NO CARRINHO**: o resumo copiavel do Checkout, a cesta da Mini loja e o copia-e-cola do Pix da pagina de obrigado sao lidos e **nao podem conter** o texto; e, com a lista e a cesta visiveis ao mesmo tempo, ele aparece **uma vez so** na tela. Este e o ponto cujo defeito seria silencioso: nada quebra, a conta continua certa. **3.** texto LONGO **quebra**, medido por **fragmentos de linha** (`Range.getClientRects()`), nunca pela altura da caixa -- que num flex estica sozinha e ja mentiu nesta mesma semana (ver `linha-de-dinheiro.mjs`) --, mais as tres formas de cortar sem erro nenhum (reticencias, `white-space`, overflow escondido). **4.** texto HOSTIL (apostrofa, aspas, barra invertida, acento, `</script`) com o **documento inteiro sobrevivendo**: a raiz do bloco com filhos prova que o `<script>` rodou ate o fim -- sem essa assercao, um literal fechado no meio faria todas as outras passarem por vacuidade. **5.** a **busca** acha o campo (o do formulario pelo id, e o da linha da lista pelo placeholder) e o resultado mostra o **valor atual**. **6.** **BACKUP ANTERIOR A ESTA RODADA**: o estado e **colhido da propria referencia** (`git archive` + a ferramenta de la gravando de verdade, molde de `zap-saldo-migracao.mjs`), e o que se cobra e o que o dono ve -- o campo de descricao **aparece vazio**, nome/preco/quantidade atravessam intactos, a barra vermelha nao acende, e regravar escreve a chave nova vazia sem mexer no resto. | Ao mexer na descricao do opcional, em `fcOpCopia`/`fcOpDescSrc`/`fcTemOpDesc`/`fcApoioCss`, nos `*OpRender`/`*OpAdd`/`*OpDuplicar`, ou ao levar opcionais a uma aba nova. **A regressao byte a byte nao alcanca este ramo de proposito**: ela mede com o campo VAZIO, que e o que prova o invariante (nenhuma saida muda um byte). O ramo em que a descricao EXISTE e medido aqui, com o bloco rodando -- prova mais forte para este campo, porque ve **onde o texto cai na tela**, e nao so que ele saiu no codigo. Nao precisa de internet. `node scripts/verificar/descricao-opcionais.mjs` (118 verificacoes). |
| `upsell.mjs` | **O UPSELL -- LEVAR O CLIENTE A OUTRA PAGINA DEPOIS DO PAGAMENTO** (13/09/2026, pedido do dono). Dois controles novos em cada uma das quatro abas de pagamento -- o endereco da pagina e um interruptor --, e a regra de emissao deles nao e "o interruptor manda": quem decide se as variaveis saem e o **ENDERECO**. Endereco vazio + desligado nao emite nada (byte a byte como antes); endereco escrito emite as duas variaveis **inclusive com o interruptor desligado**, inertes, esperando um `true`; endereco vazio + ligado e **recusado**. A terceira linha e o pedido do dono em pessoa -- *"para nao ter que ficar regerando codigo, eu poderia editar diretamente o codigo na pagina, alterando a variavel que controla esse flag"* --, e ela e exatamente o que `regressao.sh` **nao alcanca**: com a fabrica (campo vazio) a saida e identica a de antes da rodada, entao a regressao prova que nada quebrou e nao consegue provar que algo passou a funcionar. **260 verificacoes, em nove partes.** **1.** os quatro estados no TEXTO das quatro saidas, mais a propriedade que amarra as duas colunas -- ligado e desligado diferem **so** no valor do interruptor (comparados trocando uma palavra num e exigindo igualdade com o outro). **2.** a recusa, cobrando que a frase diga o que esta errado **e o que fazer**. **3.** o endereco recusado na geracao, pela MESMA regra do botao de acao da Contagem regressiva (`cUrlOk`/`urlLimpa`): `javascript:`, `data:`, `javascript:` com tabulacao no meio, barra invertida, protocol-relative e caminho sem barra inicial recusados; ancora, caminho do site e http(s) aceitos; mais um endereco hostil (aspa, barra invertida, `<b>`, `&`) que tem de atravessar o escape sem quebrar o bloco. **4.** o BLOCO EXECUTANDO, com o `onApprove` do proprio bloco chamado por uma sonda que troca o `document.head.appendChild` (o mesmo caminho de `pac-quantidade.mjs` e da previa da ferramenta): ligado leva, desligado carrega as variaveis e **nao** leva, e **o desligado com `false` trocado por `true` NO TEXTO JA GERADO, sem voltar a ferramenta, passa a levar** -- nas quatro abas. **5.** a ORDEM do gancho na Mini loja, medida **na pagina de destino**: a cesta e lida do armazenamento DEPOIS da navegacao, que e onde o defeito apareceria ("o cliente volta com a cesta cheia depois de ter pago"). **6.** o caminho C, nos dois cenarios e em duas larguras -- e ele e medido de **duas formas diferentes, porque os blocos sao diferentes**: no Checkout e na Mini loja o botao faz `window.open`, e o registro do que ele pediu vai para o `localStorage` (uma variavel da pagina morre com a navegacao -- medido, foi o primeiro tropeco desta bateria); na `/pagar` **e, desde 13/09/2026, na Agendamento por pacote**, o botao e uma **ancora com target=_blank**, e a pergunta e se a navegacao da pagina de tras cancela a acao padrao dela (nao cancela: a aba nova abre e carrega). A medicao da aba nova **nao foi herdada** da `/pagar`: o mecanismo e o mesmo, mas o bloco e outro, o botao nasce dentro de uma area escondida (so aparece depois de "Gerar Pix") e o ouvinte e pendurado noutro lugar do gerador. Sem WhatsApp configurado nao existe botao, logo nao existe caminho C, e o upsell continua valendo pelo cartao -- na Agendamento por pacote esse estado so se alcanca **editando o bloco a mao** (a aba recusa gerar sem WhatsApp), e e assim que ele e medido. **6b.** os quatro estados da tabela do upsell medidos pelo **clique no "Ja paguei"** daquela aba, e nao pelo cartao -- inclusive o que representa o pedido do dono: gerar DESLIGADO e trocar `false` por `true` no codigo publicado. **7.** o LINK de cobranca sai **identico** nos tres estados (o upsell e FIXO DA PAGINA e entra no codigo 1, nunca no link -- se viajasse, entraria no selo e derrubaria todo link novo em qualquer `/pagar` com codigo 1 antigo), e o **codigo 1 MUDA**, que e o que impede a linha anterior de ser vacuidade. **8.** o endereco invalido escrito A MAO no codigo publicado: **medicao, nao assercao** -- o bloco nao reconfere de proposito. Medido: apagar `UPSELL_URL` nao recarrega a pagina (a guarda de vazio segura) e o bloco continua na tela; um `javascript:` ali **executa** (e o dono escrevendo script na propria pagina); um caminho sem barra navega para o caminho relativo aquela pasta. **9.** o que fica GRAVADO, nas quatro abas: a recarga devolve os dois campos; o **estado ANTIGO** -- as duas chaves apagadas do armazenamento, que e exatamente o que um backup anterior a esta rodada tem -- abre com o campo vazio e o interruptor em "nao" (e nao em branco), sem erro de console; e o **preset da aba** carrega os dois. | Ao mexer em `fcUpsellUrl`/`fcUpsellEmite`/`fcUpsellRecusa`/`fcUpsellVarsSrc`/`fcUpsellFnSrc`/`fcUpsellChamada`, no gancho `aoAprovarExtra` de `fcPpBotoesSrc`, no `onApprove` proprio da `/pagar`, no `avisarZap` de qualquer aba, ou ao levar o upsell a uma aba nova. Nao precisa de internet. `node scripts/verificar/upsell.mjs`. |
| `upsell-janela.mjs` | **A ABA DO WHATSAPP SOBREVIVE A UMA NAVEGACAO IMEDIATA DA PROPRIA PAGINA?** (13/09/2026). O botao "Ja paguei" do Checkout e da Mini loja faz duas coisas no mesmo clique que disputam o navegador: abre a conversa do WhatsApp com `window.open(url,'_blank')` e, logo em seguida, manda a PROPRIA pagina para o upsell com `location.href`. Se a aba recem-aberta fosse descartada junto com o documento que a pediu, o cliente clicaria, veria a pagina trocar e o WhatsApp **nunca** abriria -- um pedido perdido sem erro nenhum na tela. Seis cenarios (so `window.open`; `window.open` + navegacao imediata, em `setTimeout` 0, 300 e 1200 ms; e sem `window.open` nenhum), em duas larguras (computador 1280x800 e celular 390x844 com user agent de Android, `isMobile` e `hasTouch`), **tres passagens cada**. Nao usa o molde, de proposito: medir navegacao exige tres enderecos de verdade na mesma origem, e o molde serve uma rota so. **Medido em 13/09/2026: a aba NUNCA e perdida** -- 2 abas no fim em todos os cenarios com `window.open`, a aba nova em `zap.html` e com o `load` disparado, `window.open` devolvendo janela nas 36 passagens, zero erros e **zero intermitencia**. O cenario 1 e o 6 existem para a medida nao ser vacua: um prova que o arnes enxerga a aba quando ela existe, o outro que enxerga a ausencia dela. **Alcance declarado:** e o motor Blink (Chromium do Playwright); nao e prova sobre o Safari do iPhone, que exigiria WebKit ou aparelho real. | Ao mexer na ordem entre `window.open` e o upsell no `avisarZap` de qualquer aba, ou ao levar esse par a um bloco novo. Nao precisa de internet. `node scripts/verificar/upsell-janela.mjs`. |
| `unificar-pagamento.mjs` | **AS QUATRO ABAS DE PAGAMENTO SOB A MESMA REGRA** (13/09/2026, leva 2). O dono pediu que os construtores com pagamento tenham as **mesmas regras e as mesmas configuracoes**; uma varredura achou **41 divergencias** entre Checkout, Mini loja, Agendamento por pacote e Link de cobranca, e esta leva fechou as que tocam **dinheiro, recusa ou link**, mais cinco decisoes dele. **Cinco partes, 68 verificacoes.** **1. O sinal fixo alto na Agendamento por pacote**: `aTotalMaximo` **nao existia**, e sinal fixo acima do maior pedido possivel gerava sem uma palavra -- toda reserva travava depois, na pagina de obrigado. Aqui se mede a CONTA e nao so a existencia da recusa: o teto **soma os opcionais no teto da quantidade** (400 passa onde o preco do pacote sozinho seria 300) e e o **MAIOR pacote, nunca a soma** (com 300 e 500, 500 passa e 800 recusa), o limite e exato (800 passa, 801 recusa), o sinal **percentual** nao e tocado, e o bloco EXECUTANDO confirma os dois lados: carrinho maximo paga, carrinho minimo recusa -- o piso de venda deliberado que `uTotalMaximo` ja registrava. **2. O codigo do pedido do Checkout**: mede primeiro o que **nao** era o defeito -- o campo JA tem `maxlength` 25, entao digitar nunca estourou o txid --, e depois a recusa nova, que e de **caractere** e e a mesma da Mini loja; mais o teto pelo caminho onde `maxlength` nao age (valor posto por ESTADO). Fecha com o bloco EXECUTANDO: a tela mostra `CAMPANHA-2026` e o campo **62>05 do payload**, lido por um **leitor TLV escrito dentro do teste** (nunca o do projeto: a maquina que monta nao pode ser a testemunha que confere), carrega `CAMPANHA2026` -- o mesmo identificador, a menos do hifen que o Pix descarta. **3. O endereco da pagina de obrigado**: a guarda aceitava `/obrigado` enquanto a frase ao lado jurava o contrario. A medicao decidiu qual dos dois lados mudava -- o unico consumidor e `aEnderecos()`, a lista que o dono cola **dentro do TidyCal**, que redireciona de fora do site --, e a guarda passou a exigir o endereco inteiro, com **recusa de nome proprio** para o caminho relativo (no molde de `pUrlRecusa`). **4. A lista do WhatsApp com e sem o resumo**: os dois blocos sao EXECUTADOS, com um opcional de **quantidade zero** no carrinho -- que e exatamente onde os dois lacos poderiam discordar --, e a mensagem que cada um monta e comparada **caractere por caractere**. **5. As cinco decisoes do dono**, uma medicao cada: preco zero recusado nas quatro (no cadastro **e** ao gerar, com o opcional a R$ 0,00 continuando valido); resumo copiavel ligado de fabrica; desconto do Pix de fabrica em 5% nas tres de catalogo e 0 no Link de cobranca; separador **neutro** (`OU`) nas quatro; passo de 0,5 no percentual do sinal nas quatro. A parte **5.4b** e a unica que compara com referencia: a **migracao** do separador ja gravado, com o estado **colhido da propria referencia**. | Ao mexer em `aTotalMaximo`/`aRecusa`/`uRecusa`/`mRecusa`, no `u-cod`, em `FC_ORD_PARES`/`FC_ORD_TXT`/`fcSepNeutro`, no `itensEscolhidos` do Checkout, ou **sempre que uma regra de uma aba de pagamento mudar sem mudar nas outras tres**. Nao precisa de internet. `node scripts/verificar/unificar-pagamento.mjs [referencia]` (padrao `main`). **A parte 5.4b so MEDE contra uma referencia ANTERIOR a 13/09/2026**; contra uma que ja tenha o separador neutro ela diz **NAO MEDIU**, com o comando para medir de verdade -- medido em 13/09/2026 contra `c1c35eb`: 68 verificacoes, tudo OK. |
| `sku-por-item.mjs` | **O SKU DE CADA LINHA -- UM POR PRODUTO, UM POR OPCIONAL** (leva 7, 14/09/2026, pedido do dono). Ele olhou a previa que a leva 3 criou e viu que **todas as linhas levavam o mesmo `sku`** -- o codigo do pedido. A coluna "ID do produto" do relatorio repetia o mesmo texto em toda linha: nao era ausencia de informacao, era **informacao que nao distingue nada com cara de que distingue**. Ele nomeou a distincao: *"Codigo do pedido e uma coisa... SKU de produto e o SKU para cada um dos itens opcionais e outra"*. Seis campos novos (produto/pacote e opcional, nas tres abas com catalogo) e **cinco decisoes**, cada uma medida aqui: **1.** SKU vazio -> a linha sai **SEM a chave** `sku` (cobrado com `hasOwnProperty`, e nao com `=== undefined`: um `sku: undefined` viraria `null` no JSON que vai ao PayPal, que e a diferenca entre omitir e mandar lixo) e **nunca** volta ao codigo do pedido, que **era o defeito**; **2.** `custom_id` continua sendo o codigo do pedido -- papeis diferentes; **3.** SKU repetido dentro da mesma aba **RECUSA gerar**, nomeando os **dois** itens em conflito, e a prova cobra tambem que **a caixa de saida nao muda** (recusa que alerta e grava mesmo assim e pior que recusa nenhuma); **4.** regra de caractere **LARGA** -- apara as pontas e corta em 127, e nada mais: o `sku` **nao entra no payload do Pix**, entao ele nao herda a regra estreita do `txid`, e cortar `ALB-20x30` para `ALB20x30` atrapalharia a conciliacao do dono; por isso `ALB20` e `alb20` **geram** (a comparacao e literal, e recusar ali seria inventar uma colisao que nao existe); **5.** a previa mostra o SKU **por linha**, numa quarta coluna, e a frase velha ("igual em todas as linhas") **nao pode ter sobrevivido** ao campo. **137 verificacoes, oito partes.** Fecha com **duplicar** (o SKU da copia vem **derivado** com `-COPIA`, como o codigo do pacote ja fazia: intacto nasceria recusado, vazio perderia o que o dono cadastrou), **backup anterior a rodada** (estado **colhido da propria ferramenta da referencia**, nunca um JSON a mao -- os itens continuam inteiros, os seis campos abrem vazios e o bloco gerado dali nao emite `sku` em linha nenhuma, medido tambem com o bloco **rodando**) e **texto hostil no SKU** (apostrofo, aspas duplas, barra invertida e `</script`), chegando **byte a byte** ao `createOrder`. **A segunda opiniao e o metodo:** a limpeza (`aparar + cortar em 127`) e **reescrita a mao** dentro do arquivo -- se os dois lados limpassem pelo mesmo caminho, o teste nao teria opiniao nenhuma. | Ao mexer em `fcSkuLimpo`/`fcSkuSrc`/`fcTemSku`/`fcSkuRecusa`/`fcSkuDup`/`fcOpsDup`, nos seis campos de SKU, no `itensEscolhidos` de qualquer aba de catalogo, ou ao levar SKU a uma aba nova. Nao precisa de internet. Portas 8601-8699. `node scripts/verificar/sku-por-item.mjs [referencia]` (padrao `66b1cb9`, a ultima antes da leva 7). **Referencia que ja tenha os campos de SKU faz a parte 7 dizer "NAO MEDIU"** -- e nao falhar, porque vermelho permanente esconde o proximo vermelho de verdade. |
| `paypal-itens.mjs` | **O PRODUTO E OS OPCIONAIS NO RELATORIO DO PAYPAL** (13/09/2026, leva 3). O dono perguntou se os construtores com PayPal estavam levando **o produto e seus opcionais** para la. Nao estavam: as tres abas com catalogo mandavam **um item so**, com os nomes concatenados num campo de 127 caracteres, e no Checkout e na Mini loja os **opcionais nao apareciam em lugar nenhum** -- entravam no valor e sumiam. Agora vai **um item por linha**, com quantidade e preco unitario. **A armadilha que governa a rodada, lida da especificacao OpenAPI do PayPal (`checkout_orders_v2`, `info.version` 2.32) e nao de memoria:** `AMOUNT_MISMATCH` ("Should equal item_total plus tax_total plus shipping...") e `ITEM_TOTAL_MISMATCH` ("Should equal sum of (unit_amount * quantity)...") sao **422 UNPROCESSABLE_ENTITY** -- o pedido **nao e criado**, e o cliente fica **sem meio de pagamento**. Nao e aviso no recibo. **O desenho que fecha a conta por construcao:** `item_total` e a **soma dos itens em centavos inteiros** (nunca `subtotal()`, que soma os mesmos numeros em outra ordem) e `discount` e a **DIFERENCA** `item_total - amount` (nunca o percentual do cupom recalculado, que reintroduziria o meio centavo de ago/2026). Assim `amount = item_total - discount` e identidade aritmetica, qualquer que seja a regra que produziu o total -- e o caso que mais castiga isso, o cupom `pct_produto` (que desconta so os produtos, ignorando os opcionais que estao dentro do `item_total`), esta medido. **357 verificacoes.** **1.** forma de cada item (`name` <=127, `quantity` no padrao `^[1-9][0-9]{0,9}$`, `unit_amount` com duas casas). **2 e 3.** as duas igualdades, **em centavos inteiros e somadas pelo TESTE**, nunca lendo o campo que o bloco escreveu. **4.** `amount` e o mesmo numero impresso na tela -- com o **desconto do Pix LIGADO** nas tres abas, provando que ele nao move um centavo do pedido do cartao. **5.** a aritmetica exata, **cercada**: bate na bala, ou difere **um centavo** e **so** na janela em que o desconto cru cai em meio centavo exato (causa medida e ANTERIOR a esta rodada: `descontoAtual()` multiplica o subtotal em ponto flutuante; os casos que divergiram sao listados no fim da saida, com os numeros). **6.** a lista e exatamente o que o cliente escolheu. **7.** o **opcional de R$ 0,00 fica de fora** -- decisao medida, nao presumida: a especificacao permite `unit_amount` "0.00" (o unico limite escrito e "can not be a negative number"), mas a lista de erros 422 traz um `CANNOT_BE_ZERO_OR_NEGATIVE` sem dizer a que campos se aplica, e apostar num comportamento **nao documentado** com o pedido inteiro em jogo custaria o pagamento do cliente. **8.** `custom_id` continua sendo o codigo do pedido -- e, **desde a leva 7 (14/09/2026), NENHUMA linha leva `sku`** aqui, porque este cenario nao cadastra SKU nenhum. E de proposito: o que se cobra neste arquivo e o caminho VAZIO, a decisao 1 daquela rodada (SKU em branco sai **sem** o campo, e **nunca** com o codigo do pedido, que era o defeito relatado pelo dono). O caminho **com** SKU cadastrado -- um por produto, um por opcional, a recusa de repetido e a previa -- mora em `sku-por-item.mjs`, ao lado. **A DECISAO SOBRE O SINAL:** com sinal o cliente paga uma fracao e o formato do PayPal **nao tem campo para entrada** -- forcar a diferenca para `discount` faria o recibo chamar de DESCONTO o saldo que ele ainda deve. As tres abas continuam mandando a **linha unica**, e o teste prova os dois lados (um item so, nenhum `discount` inventado). **Os CASOS sao os subconjuntos que `sinal.mjs` catalogou**, com o criterio **declarado e reconferido caso a caso**: a familia daquela varredura nasceu do sinal de 50%, e com sinal nao se itemiza -- entao a familia relevante aqui e **derivada** (cupom de 25% sobre subtotal de C centavos desconta C/4; cai em meio centavo quando `C mod 4 === 2`), com um **controle fora dela**. Fecha com **texto hostil** (apostrofa, aspas duplas, barra invertida, acento e `</script`) em nome de produto **e** de opcional, chegando **byte a byte** ao pedido. **E a comparacao com a referencia PRESA**: com sinal, o `purchase_unit` sai **identico** ao da referencia **fora o `sku`**, e a unica diferenca deliberada (a linha unica deixou de levar o codigo do pedido no `sku`) e cobrada **nomeada, nos dois sentidos** -- a referencia levava, a arvore de hoje nao leva -- em vez de ficar escondida dentro de um "identico" que nao seria identico; sem sinal, os **itens mudaram de proposito** e a **`description` nao mudou um caractere** -- ela sai de `nomesSelecionados()`, que foi reescrita para derivar da enumeracao unica. | Ao mexer em `fcPpItensSrc`/`ppDetalhar`, em `fcPpBotoesSrc`, no `itensEscolhidos` de qualquer das tres abas de catalogo, em `FC_PP_MAX_ITENS`/`FC_PP_MAX_QTD`/`FC_LIM_PP`, ou ao levar PayPal a uma aba nova. Nao precisa de internet. `node scripts/verificar/paypal-itens.mjs [referencia]` (padrao `d5c44ed`, a ultima antes da leva 3). **Referencia ausente ou incompativel faz a ultima secao dizer "NAO MEDIU" e FALHAR** -- prova que nao mede conta como cobertura, e e pior que prova nenhuma. |
| `novidades.mjs` | **O PAINEL NOVIDADES -- o que mudou em cada versao publicada** (14/09/2026, pedido do dono). O terceiro painel da barra do topo, ao lado de "Detalhes" e "Identidade", com as versoes agrupadas por dia, da mais recente para a mais antiga. E a unica parte da ferramenta cujo conteudo e **texto escrito a mao sobre o proprio historico**: ele nao gera nada, entao `regressao.sh` **nao o alcanca** -- lá o invariante e "nenhuma saida mudou", e uma lista de novidades parada em agosto o satisfaz perfeitamente. **38 verificacoes.** A que mais vale e a primeira: as versoes **desenhadas na tela** (lidas pelo atributo `data-v`, porque a ferramenta inteira vive dentro de um IIFE e `FCR_NOTAS` nao e alcancavel de fora) sao comparadas com **todo valor que `FC_VERSAO` ja teve no `index.html`, lido do git a cada execucao**. Publicar uma versao sem descreve-la **falha aqui, com o numero dela na tela**. A unica excecao aceita e **uma** entrada fora do git, no topo, com a data de **hoje** -- a versao que a rodada em curso ainda vai publicar; qualquer outra combinacao e engano, nao entrega em andamento. **Esta prova nao tem referencia congelada e nao pode envelhecer:** a fonte de verdade e o git do proprio repositorio. As outras partes: o painel nasce fechado, abre e fecha; so o dia mais recente nasce aberto e a versao mais recente e a **primeira visivel antes de expandir nada**; os **tres** paineis da barra nunca ficam abertos juntos (os dois antigos so sabiam fechar um ao outro); todo rotulo de item cai numa cor declarada (rotulo novo sem cor **aparece** aqui, em vez de sair cinza calado); e -- a que protege o resto da ferramenta -- **as secoes de texto das abas continuam alternando** depois de `fcdLigar` ser chamada de novo, que e o preco de o painel criar secoes recolhiveis apos a partida: sem a marca `data-fcd` o cabecalho ganharia um segundo ouvinte, o clique alternaria duas vezes e a secao **nunca mais abriria, sem erro nenhum no console**. Fecha medindo, e nao supondo, que **a busca do topo nao alcanca as novidades**: ela varre campos de formulario, o painel nao tem nenhum, e uma frase que so existe ali devolve "nenhum resultado". E **exercita as duas redes que a regra do dono de 14/09/2026 exigiu** (*"sempre que alterar algo no projeto, seja melhoria, seja correcao, o release notes tem que SEMPRE ser atualizado"*): a arvore e servida uma segunda vez com **um byte trocado** (`FC_VERSAO` apontando para uma versao que a lista nao descreve), e tem de acender **a barra vermelha da partida** -- que aparece sem ninguem abrir o painel, e alcanca justamente quem esqueceu de escrever a nota -- **e** o aviso dentro do painel, os dois **nomeando** a versao e dizendo onde escrever. Depois roda `scripts/conferir-versoes.sh` sobre copias adulteradas para provar que o **portao de antes de publicar** recusa carimbo sem nota e recusa a mesma versao descrita duas vezes -- detector que ninguem dispara e promessa, nao medicao. Cobre tambem a **barra vermelha da partida**, que `prep()` levanta sem escrever nada no console: sem essa linha, uma falha do proprio painel passaria por "nenhum erro". | Ao acrescentar uma versao a `FCR_NOTAS`, ao mexer em `fcrDesenhar`/`fcrItem`/`fcrSecao`/`fcrPainelAbrir`, em `fcdLigar`, ou ao criar um quarto painel na barra do topo. Nao precisa de internet. `node scripts/verificar/novidades.mjs [porta]`. |
| `paypal-previa.mjs` | **A PREVIA DO RELATORIO DO PAYPAL MENTE?** (13/09/2026, leva 3). As quatro abas com PayPal passaram a mostrar, dentro do quadro da previa, os campos que de fato vao para o pedido: a lista de itens com nome, **SKU**, quantidade e preco unitario, o `item_total`, o `discount`, o `amount.value`, a `description` e o `custom_id`. **O SKU e uma COLUNA da tabela desde a leva 7 (14/09/2026)**, e nao mais uma linha unica la embaixo dizendo que ele era "igual em todas as linhas" -- o cenario cadastra SKU em um produto e no opcional e deixa o OUTRO produto sem nenhum, de modo que a mesma passagem cobre o valor escrito **e** o traco que anuncia a ausencia do campo. Previa que **remonta** o resultado com codigo proprio e a segunda implementacao que este projeto recusa (a Contagem regressiva divergiu duas vezes assim) -- e aqui isso teria um segundo dente: uma previa que montasse a lista sozinha poderia mostrar uma conta que fecha enquanto o bloco manda uma que nao fecha, e o defeito so apareceria no cliente, sem meio de pagamento. **29 verificacoes, dois caminhos independentes para o mesmo bloco:** o **lado A** e a previa da ferramenta, com o bloco rodando dentro do iframe e o relatorio **lido da tela**, exatamente como o dono o ve; o **lado B** e o **mesmo texto de bloco** executado no molde comum, com o `createOrder` chamado direto. Mesma configuracao, mesmo carrinho, comparacao **campo a campo** -- itens (nome, **SKU**, quantidade, `unit_amount`) e os quatro campos de valor e conciliacao --, mais a recusa de **inventar** a linha de desconto quando o pedido nao tem nenhum. **As duas formas da sonda entram de proposito**, porque desde 13/09/2026 ela e **uma so** para as quatro abas: o Checkout chama `render(elemento)`, tem `onClick` e reage a cada clique; o Link de cobranca chama `render('#fcpg-pp')` -- **seletor em texto** --, nao tem `onClick` e nao tem raiz a que reagir. Era ali que morava a **copia manuscrita** de `fcPvSondaPP` que esta leva apagou; se a unificacao tivesse quebrado uma das duas diferencas (o seletor em texto, ou a marca `fcPvSemPayPal` que o shim daquela aba confere), a previa dela **nem desenharia** -- e as duas coisas sao medidas. | Ao mexer em `fcPvSondaPP`, em `pPvPayPal`, no que a previa mostra do pedido, ou ao levar a previa do PayPal a uma aba nova. Nao precisa de internet. `node scripts/verificar/paypal-previa.mjs`. |
| `aparencia.mjs` | **A APARENCIA DAS QUATRO ABAS DE PAGAMENTO, MEDIDA NO DOM** (13/09/2026, leva 5 -- a ultima da unificacao). Aparencia nao se confere lendo o CSS emitido: uma regra pode existir e nao alcancar o elemento, outra pode ser vencida por uma irma de mesma especificidade escrita depois, e `text-align` pode chegar por HERANCA de um curinga que nem cita a classe (`.fca-ob-raiz *`). Aqui tudo e `getComputedStyle` sobre o bloco **rodando**, nas quatro abas, em **1024 e 375 px** (a largura do aparelho do dono) e em **duas passagens** -- com sinal e com desconto no Pix, que nao cabem na mesma, porque o sinal zera o desconto. **201 verificacoes.** Cobra o que a leva ALINHOU: o aviso "o Pix nao confirma sozinho" com o mesmo veu e **sem cor fixa** nas quatro (as quatro tem a cor do texto configuravel pelo dono); o Copia e Cola com a mesma altura, a mesma fonte e **sem redimensionar**; a area do Pix **a esquerda** nas quatro (item 8 do Manual do Prosite); o saldo **sem negrito**; e as tres respostas do sinal com **margem**, nao enchimento. Mede tambem o que NAO pode quebrar: cada caixa com area na tela, e -- ao contrario -- que **sem a biblioteca do QR a caixa SOME nas quatro**, que e o desenho, e nao um defeito. **A chapa branca do QR (item 6) tem secao propria, com a rede aberta para o cdnjs**, porque so a biblioteca de verdade responde se a chapa e decoracao: medido, o canvas sai com **zero pixel branco nas bordas**, entao o `padding:10px` da chapa e a unica zona quieta do codigo -- e a assercao falha no dia em que a biblioteca mudar, reabrindo o item com medicao nova em vez de inercia. Uma secao a parte nao olha bloco nenhum e sim a **ferramenta**: as **nove previas** (em oito abas) desenhando dentro da classe unica `.pv-moldura`, com a cobranca de que **nenhuma copia com prefixo de aba voltou a nascer** -- que e como a duplicacao do item 14 se reinstalaria. Fecha com a **prova do contrario**, presa em `6eabd1f`: a versao de antes tinha as cinco divergencias, e se a referencia ja tiver a leva 5 dentro ele diz **NAO MEDIU**, nunca falha por envelhecimento. **AS TRES ARMADILHAS DE MEDICAO ESTAO NO CABECALHO DO ARQUIVO**, e uma delas mordeu durante a propria escrita: `getComputedStyle` de elemento dentro de ancestral `display:none` devolve o dele, e a area do Pix do Checkout **nunca abria** -- 30 valores foram lidos de elementos escondidos antes de a guarda "a area do Pix abriu de verdade" existir. | Ao mexer em qualquer regra de CSS das quatro abas de pagamento, em `fcPixManualCss`, na chapa do QR, ou ao levar pagamento a uma aba nova. **Uma secao usa a rede** (cdnjs, para o QR de verdade); as outras nao. `node scripts/verificar/aparencia.mjs [referencia]` (padrao `6eabd1f`); `FC_DUMP=1` imprime a tabela crua de tudo o que foi medido. |
| `unificar-config.mjs` | **O QUE O DONO CONFIGURA, SOB A MESMA REGRA NAS QUATRO ABAS** (13/09/2026, leva 4). A leva pegou o que o dono configura e nao tinha efeito, e os textos duplicados -- e quase tudo nela tinha contrato de **nao mudar um byte** de saida, o que deixa a regressao sem nada a dizer. **57 verificacoes, em duas metades declaradas.** A metade do **ARQUIVO** (le o fonte, nao abre navegador) mede o que e "esta escrito um lugar so": que os **75 campos numericos** dizem a mesma faixa na tabela da aba e no `<input>` (eram tres que discordavam -- o sinal fixo sem `max` no HTML); que as frases que duas tabelas escreviam a mao (`Codigo copiado! Cole no aplicativo do seu banco.`, o `nao consegui copiar`, o `Pagamento aprovado. Obrigado!`) aparecem **uma** vez; que as **dez linhas do WhatsApp** deixaram de ser trinta linhas manuscritas e viraram `fcZapTxtDefs`; que `aRecusa` **nao chama mais `fciVal`/`fciBruto`**; e que as recusas do `fc-compartilhado.js` sairam **acentuadas** com a versao trocada nos QUATRO lugares. A metade do **NAVEGADOR** mede comportamento: o **prefixo de conciliacao** se corrigindo a vista ao ser digitado e sendo **recusado com nome** quando chega por estado gravado (os dois sentidos); os **contadores** dos tres campos que faltavam na tabela de limites, com a prova de que o prefixo e o codigo do pacote **somam o mesmo orcamento** e ficam vermelhos juntos, e de que nenhum dos dois ganhou `maxlength` (cortaria a digitacao de um codigo hifenizado que cabe); a recusa do WhatsApp na aba pac **nos dois sentidos**; o **teto do pedido** da Mini loja lendo `1200,50` de um estado gravado (sinal fixo de 1200,30 **aceito** e de 1200,60 **recusado** -- antes desta rodada o teto virava 1200 e o primeiro era recusado); e a **unica mudanca de saida da rodada**: a Mini loja **sem cupom cadastrado** passou a emitir a maquina do cupom, como as tres irmas sempre fizeram. Essa ultima e provada com o **bloco rodando**, nos dois estados -- sem cupom a caixa nasce escondida e o cliente nao ve nada a mais; com um cupom escrito **a mao dentro do bloco publicado** (que e o caminho que o recorte anterior matava em silencio) a caixa aparece, o cupom e aceito, a linha de desconto acende e o total baixa. **Nao compara com referencia nenhuma**, de proposito: mede propriedade, e propriedade nao envelhece no dia em que a rodada e mesclada. | Ao mexer em `FC_LIM_CAMPOS`, `fcNumConferir`/`fcNumTabelas`, `fcZapTxtDefs`, `FC_TXT_FABRICA`, `aPrefixoAjustar`, `aRecusa`/`aCfg`, `mTotalMaximo`, no cupom da Mini loja, ou nas recusas de `fc-compartilhado.js`. Precisa do Playwright para a segunda metade. `node scripts/verificar/unificar-config.mjs` |
| `chave-pix-limpeza.mjs` | **A CHAVE PIX DA ABA AGENDAMENTO POR PACOTE: UMA LIMPEZA SO** (leva 6, 14/09/2026), com os blocos RODANDO e com o lado **ANTES** medido de verdade. Ate esta rodada `aRecusa` validava `cfg.chave` (nascida de `pixLimpar`) e o gerador emitia `fciVal('chave')` (que passa por `fcTrim`): conjuntos DIFERENTES de invisiveis, e chave com invisivel na ponta era **validada por uma string e cobrada por outra**. **Quatro partes. 1.** o DEFEITO na referencia (`7e2baec` por padrao -- nunca `main`, que depois da mesclagem faria o lado "antes" medir a si mesmo): uma configuracao ACEITA produz **dois BR Codes diferentes**, o da previa com o invisivel e o da textarea sem ele, os dois lidos por um **TLV e um CRC16 escritos dentro do arquivo**. **2.** a mesma configuracao na arvore de hoje: um BR Code so. **3.** o invisivel no MEIO continua RECUSADO -- o conserto nao afrouxou nada. **4.** varredura estatica das **outras tres abas e da `/cobrar`**, que ja usavam a mesma limpeza nas duas pontas. O caminho que ALCANCA o defeito e a **colagem** (`input` nao corrige o campo; `change`/`blur` corrigem, e a partida da ferramenta tambem) -- medido, e registrado no cabecalho porque a primeira versao do arquivo nao alcancava o estado. | Ao mexer em qualquer ponta da chave Pix, da recusa ou do payload. |
| `chaves-renomeadas.mjs` | **AS CHAVES DE TEXTO QUE TROCARAM DE NOME, E A CONVERSAO** (levas 6 e 7, 14/09/2026). `txtPixRotulo` -> `txtSecaoPix` na Agendamento por pacote (era a unica chave da ferramenta com DOIS significados) e `t4` -> `txtZapBotao` no Link de cobranca (o unico "Ja paguei" fora da familia `txtZap*`). Renomear sem converter ZERA o campo de quem personalizou, **em silencio**, e alcanca o backup em arquivo -- a regressao byte a byte nao pega isso. **Na leva 7, por analogia a mesma autorizacao do dono, entrou o terceiro caso da mesma familia:** `txtCopiado` -> `txtPixCopiado` e `txtNaocopiou` -> `txtPixNaocopiou`, tambem no Link de cobranca -- as outras tres abas ja chamavam este par assim, com a mesma fabrica, para o mesmo texto. Cada renome **detecta o proprio envelhecimento**, e nao o da rodada vizinha: uma referencia escolhida ENTRE as duas levas teria os nomes novos de uma e os velhos da outra, e a parte que nao puder medir diz `NAO MEDIU`. **Quatro partes:** texto PERSONALIZADO colhido da propria ferramenta da referencia atravessa inteiro (e chega a tela do cliente, com os **dois blocos rodando**); texto de FABRICA continua de fabrica, sem barra vermelha; estado que JA tem a chave nova nao e tocado; e a varredura do fonte de hoje, que cobra tambem que `txtPixRotulo` **continua** nas tres abas onde ele significa a linha do preco. Referencia presa em `7e2baec`, com `NAO MEDIU` quando ela envelhecer. | Sempre que uma chave de texto trocar de nome. |
| `frase-copiado.mjs` | **A FRASE DO "CODIGO COPIADO", UMA SO NAS QUATRO ABAS** (leva 6, 14/09/2026, decisao do dono). As quatro passam a dizer `Código copiado! Cole no aplicativo do seu banco.` -- e o momento em que o cliente esta prestes a sair da pagina, e a frase diz o que fazer em seguida. **Quatro partes. 1.** os quatro campos nascem na frase longa e a barra vermelha de fabrica divergente nao acende (o `value=` do HTML e a segunda copia da fabrica). **2.** os **QUATRO blocos EXECUTANDO**: nas duas abas que escrevem a confirmacao no carregamento le-se o elemento; nas duas que a escrevem dentro do ouvinte, **clica-se** e le-se a tela. **3.** quem JA usou a ferramenta continua com a frase curta, com estado colhido da referencia -- **e isso e o desejado**, e nao um defeito: a frase curta continua CERTA, so mais curta, ao contrario do separador "OU", que ficaria ERRADO e por isso precisou de migracao. **4.** uma entrada de fabrica, e nao duas. Referencia presa em `7e2baec`, com `NAO MEDIU`. | Ao trocar um padrao de fabrica de texto. E antes de copiar a migracao do separador por analogia. |
| `qr-configuravel.mjs` | **O TAMANHO DO QR CODE NAS QUATRO ABAS** (leva 9, 14/09/2026, autorizado pelo dono). Ate esta rodada o campo existia numa aba so (`p-qr`, na Link de cobranca, 120 a 320, padrao 180) e as outras tres emitiam **fixo em 200**. Agora as quatro tem campo -- `u-qr`, `m-qr`, `a-qr`, **mesma faixa e mesmo passo**, e **padrao de fabrica 200**, que e o numero que elas ja emitiam: padrao que muda a saida sem ninguem pedir e exatamente o que `regressao.sh` existe para denunciar, e a regressao contra `46ab2fd` fecha em **zero divergencia**. **82 verificacoes, em quatro partes. 1.** o campo chega ao QR **desenhado**, medido por `canvas.width` com a **qrcodejs de verdade** (a rede abre para um host so, o cdnjs, pelo mesmo motivo ja aceito em `aparencia.mjs`: quem decide o tamanho do canvas e a biblioteca, e transcrever o que ela faz seria opiniao). Os tres numeros da passagem trocada sao **diferentes entre si** -- 140, 260 e 320 --, de modo que um bloco lendo o campo da aba errada falha; e a Link de cobranca entra como **controle**, continuando nos 180. **2.** a faixa se corrige **no `change`**, e NAO no `input` -- as duas metades sao medidas, porque corrigir a cada tecla impediria digitar `320` e clamp silencioso troca defeito visivel por invisivel. Quatro entradas por aba (999, 5, 0 e `abc`); a coluna do que o campo mostra antes do `change` e declarada, porque `<input type="number">` **recusa** texto e devolve `''`. **3.** o **acrescimo puro**: o estado e o preset sao **colhidos da propria referencia** (a arvore de `46ab2fd` exercitada num navegador, com um preset salvo em cada aba) e abertos na arvore de hoje -- o campo cai nos 200, e aplicar aquele preset **devolve** os 200 sobre um campo sujado de proposito. **4.** a busca do topo acha os tres campos, pelo rotulo e pelo id. As partes 1, 2 e 4 medem propriedade e nao envelhecem; a parte 3 le do **proprio arquivo da referencia** se ela ja tem `id="u-qr"` dentro e, se tiver, diz **NAO MEDIU** com a linha de comando que mediria de verdade, sem falhar. | Ao mexer em `fcDesenharQrSrc`, nas tabelas `U_NUMS`/`M_NUMS`/`A_NUMS`/`P_NUMS`, ou ao levar o QR do Pix a uma aba nova. **Uma parte usa a rede** (cdnjs). `node scripts/verificar/qr-configuravel.mjs [referencia]` (padrao `46ab2fd`). |
| `pagina.mjs` | O MOLDE reutilizavel para pegar um bloco gerado e executa-lo de verdade numa pagina que imita uma do Prosite (servidor de uma rota, rede externa bloqueada -- com as excecoes declaradas por `permitir` (hosts), `bloquear` (pedacos de URL) e, desde 04/09/2026, `retardar` (pedacos de URL com um atraso em ms) --, relogio falso opcional, `reducedMotion` opcional). **`retardar` existe para defeito que so aparece numa ORDEM de chegada**, e nao na ausencia de uma das partes: a corrida entre o `embed.js` do TidyCal e o aperto de mao proprio e o caso, e sem ele a unica coisa mensuravel seria a frequencia com que a rede produz o defeito -- que foi o que deixou essa divida seis meses sem nome. Exporta `comBlocoNaPagina`, `gerarNaFerramenta`, `textoSemScripts`, `chk`, `resumo`. | Quando o teste precisa que o bloco RODE num DOM (anima? o botao aparece? o valor calculado bate?), nao so que o texto gerado seja igual a uma referencia. Cada teste concreto e um script pequeno que importa este modulo -- ver exemplo abaixo. |

## Como rodar

Precisa de Node e do Playwright com o Chromium baixado -- `lib.mjs` diz o que
instalar se faltar (`npx playwright install chromium`, ou apontar um
Chromium/Playwright ja existente com `FC_CHROME`/`FC_PLAYWRIGHT`).

```sh
# regressao byte a byte contra a main -- as DUAS passagens, num comando so
scripts/verificar/regressao.sh

# regressao contra outro commit/branch
scripts/verificar/regressao.sh algum-commit

# o upsell: os quatro estados, a recusa, o esquema, a ordem do gancho e o caminho C
node scripts/verificar/upsell.mjs

# a aba do WhatsApp sobrevive a navegacao imediata da propria pagina? (so navegador)
node scripts/verificar/upsell-janela.mjs

# os blocos com os textos de escape, executando de verdade
node scripts/verificar/textos-escape.mjs

# o sinal no Link de cobranca, e a travessia de versoes (links ja enviados)
node scripts/verificar/sinal-cobranca.mjs

# os tres textos que explicam o sinal, nas quatro abas de pagamento
node scripts/verificar/textos-sinal.mjs

# o valor minimo do cupom, com os tres blocos rodando de verdade
node scripts/verificar/cupom-minimo.mjs

# o editor em linha da lista de cupons, nas tres abas
node scripts/verificar/lista-cupons.mjs

# a quantidade do opcional no que vai para o PayPal, com o bloco rodando
node scripts/verificar/pac-quantidade.mjs

# o botao de duplicar: pacote, opcional, e o original que nao pode ser sobrescrito
node scripts/verificar/duplicar-itens.mjs
# o mesmo teste apontado para a main, para ver os casos falharem la
git worktree add --detach /tmp/ref main && node scripts/verificar/duplicar-itens.mjs /tmp/ref

# o formulario de cadastro: a recarga devolve, o preset zera (as tres abas)
node scripts/verificar/preset-formulario.mjs
# o mesmo teste apontado para uma arvore de referencia, para ver o defeito falhar la
git worktree add /tmp/ref main && node scripts/verificar/preset-formulario.mjs /tmp/ref

# o filtro de origem do TidyCal, a guarda do campo e a migracao
node scripts/verificar/tidycal-origem.mjs

# a altura automatica do calendario, nos dois blocos que criam o proprio iframe
node scripts/verificar/tidycal-altura.mjs

# o calendario unificado, contra o TidyCal de verdade (PRECISA DE INTERNET)
node scripts/verificar/tidycal-unificado.mjs
node scripts/verificar/tidycal-unificado.mjs algum-commit

# o pre-carregamento do calendario, os tres modos e o e.source
# (PRECISA DE INTERNET nas partes 3 a 6; as partes 1, 2, 7 e 8 correm com a rede fechada)
node scripts/verificar/calendario-aquecido.mjs
node scripts/verificar/calendario-aquecido.mjs algum-commit

# a corrida entre o embed.js do TidyCal e o nosso aperto de mao (PRECISA DE INTERNET)
node scripts/verificar/corrida-calendario.mjs
FC_PARTES=2 FC_PASSAGENS=8 node scripts/verificar/corrida-calendario.mjs

# os limites visiveis nos campos que alimentam API: contador, maxlength, filtro e cursor
node scripts/verificar/limites-visiveis.mjs

# o orcamento do identificador de conciliacao: a colisao e a truncagem, antes e depois
# (sem argumento a referencia e 94042b6, o commit ANTERIOR ao conserto -- e nao 'main',
#  que desde 11/09/2026 ja o carrega e faria o lado "antes" medir a si mesmo)
node scripts/verificar/id-orcamento.mjs
node scripts/verificar/id-orcamento.mjs algum-commit

# a ordem dos meios de pagamento no DOM, nas duas escolhas e nas quatro abas
node scripts/verificar/meio-prioritario.mjs

# a cobranca de sinal nas TRES abas que a tem: a conta, as duas pontas de pagamento, o
# arredondamento, as recusas e -- so na Agendamento por pacote -- o identificador de
# conciliacao imune ao sinal, o encontro com o meio prioritario e a parcela sobre o sinal
node scripts/verificar/sinal.mjs

# a migracao do meio prioritario, e o outro lado da regressao (byte a byte com 'cartao')
# ATENCAO: as partes 1, 2 e 4 dele so MEDEM contra uma referencia ANTERIOR a 12/09/2026.
# E UMA LINHA DA PARTE 4 (byte a byte da vitrine da Agendamento por pacote) ficou SEM commit
# que a responda desde a leva 5 -- ela pede uma referencia anterior a 12/09 e posterior a
# 13/09 ao mesmo tempo. Desde 14/09/2026 ela diz "NAO MEDIU" em vez de falhar todo dia.
# Contra 'main' (que ja tem a rodada) ele diz "NAO MEDIU" nas duas primeiras e troca a
# pergunta da quarta -- de proposito, desde 13/09/2026: ate ali ele acusava nove falhas
# todo dia sem nenhum defeito por tras, e vermelho que e sempre vermelho esconde o proximo.
node scripts/verificar/meio-prio-migracao.mjs
node scripts/verificar/meio-prio-migracao.mjs 4c66719   # a ultima antes da rodada
node scripts/verificar/zap-saldo-migracao.mjs 4c66719   # idem, para a linha do saldo

# as quatro abas de pagamento sob a mesma regra (leva 2): a recusa do sinal fixo alto na
# Agendamento por pacote, o codigo do pedido do Checkout (tela x campo 62>05 do payload), o
# endereco relativo da pagina de obrigado, a lista do WhatsApp com e sem resumo, e as cinco
# decisoes do dono. A parte 5.4b (a migracao do separador) so MEDE contra referencia anterior
# a 13/09/2026; contra uma que ja tenha o separador neutro ela diz "NAO MEDIU".
node scripts/verificar/unificar-pagamento.mjs
node scripts/verificar/unificar-pagamento.mjs c1c35eb   # a ultima antes da leva 2

# o SKU de cada linha (leva 7): um por produto e um por opcional, vazio sai SEM o campo,
# repetido RECUSA nomeando os dois, e a previa mostra o SKU por linha. A parte 7 (backup
# anterior a rodada) esta presa em 66b1cb9 e diz "NAO MEDIU" quando a referencia envelhecer.
node scripts/verificar/sku-por-item.mjs
node scripts/verificar/sku-por-item.mjs algum-commit

# o produto e os opcionais no relatorio do PayPal (leva 3): a conta fecha ao centavo em
# centavos inteiros, o desconto sai da DIFERENCA, o opcional de R$ 0,00 fica de fora, e com
# sinal continua indo a linha unica. A ultima secao compara com a referencia PRESA e diz
# "NAO MEDIU" (falhando) se ela nao existir mais.
node scripts/verificar/paypal-itens.mjs

# a APARENCIA das quatro abas de pagamento (leva 5), medida no DOM com o bloco rodando, a
# 1024 e a 375 px. Uma secao abre a rede para o cdnjs, para desenhar o QR de verdade e medir
# se a chapa branca e decoracao ou zona quieta. A prova do contrario esta presa em 6eabd1f e
# diz "NAO MEDIU" quando a referencia ja tem a leva 5 dentro.
node scripts/verificar/aparencia.mjs
FC_DUMP=1 node scripts/verificar/aparencia.mjs   # imprime a tabela crua do que foi medido

# a previa do relatorio, comparada campo a campo com o createOrder do proprio bloco
node scripts/verificar/paypal-previa.mjs

# o painel Novidades: as versoes na TELA batem com as que o git conhece, o painel abre e
# fecha, a mais recente vem primeiro e as secoes das abas continuam alternando.
# Sem referencia congelada -- a fonte de verdade e o git, lido a cada execucao.
node scripts/verificar/novidades.mjs

# a chave Pix da aba Agendamento por pacote: o defeito ANTES, o conserto DEPOIS, com os
# blocos rodando e um leitor TLV escrito dentro do arquivo
# (sem argumento a referencia e 7e2baec, o commit ANTERIOR a leva 6 -- e nao 'main', que
#  depois da mesclagem faria o lado "antes" medir a si mesmo; ai a parte 1 diz "NAO MEDIU")
node scripts/verificar/chave-pix-limpeza.mjs
node scripts/verificar/chave-pix-limpeza.mjs algum-commit

# as chaves de texto que trocaram de nome (levas 6 e 7), e a conversao que salva o texto do dono
# (idem: referencia presa em 7e2baec, com "NAO MEDIU" quando ela envelhecer)
node scripts/verificar/chaves-renomeadas.mjs

# a frase do "codigo copiado" nas quatro abas, com os quatro blocos rodando
node scripts/verificar/frase-copiado.mjs

# o tamanho do QR Code nas quatro abas (leva 9): o canvas DESENHADO segue o campo, a faixa
# se corrige no change e nao no input, e backup/preset anteriores a rodada caem no padrao 200.
# A parte 3 esta presa em 46ab2fd e diz "NAO MEDIU" quando a referencia envelhecer.
node scripts/verificar/qr-configuravel.mjs
node scripts/verificar/qr-configuravel.mjs algum-commit

# a busca da ferramenta e o eco do texto nos rotulos: o caso do dono, ponta a ponta
node scripts/verificar/achar-textos.mjs

# os textos que entraram nas tabelas: a chave gravada, o eco, a reposicao e a APOSTROFA
node scripts/verificar/textos-migrados.mjs
node scripts/verificar/textos-migrados.mjs algum-commit

# os nove textos reserva dos marcadores: a MIGRACAO do formato gravado (backup antigo,
# preset antigo), a ordem do endereco e o caractere de controle -- medido, nao suposto
node scripts/verificar/textos-reserva.mjs
node scripts/verificar/textos-reserva.mjs algum-commit

# a descricao de cada item opcional: os tres estados, o carrinho limpo e o backup antigo
node scripts/verificar/descricao-opcionais.mjs
node scripts/verificar/descricao-opcionais.mjs algum-commit   # a referencia da parte 6

# o acento que falta no texto que uma pessoa le (sai 1 se achar algo)
node scripts/verificar/acentos.mjs
node scripts/verificar/acentos.mjs --vocabulario

# um teste novo que use o molde de pagina.mjs (exemplo, nao existe no repo)
node scripts/verificar/teste-bordas.mjs
```

## As duas passagens da fotografia

Desde 03/09/2026 `geradores.mjs` roda o cenario DUAS vezes:

- **fabrica** -- todos os textos no padrao. E a passagem historica, e prova o
  invariante: mexer numa aba nao muda um byte do que as outras geram.
- **configurada** -- 28 campos `*-txt-*` preenchidos, escolhidos por criterio:
  um por aba (as dez, menos Bordas e Efeitos de pagina, que **nao tem nenhum**
  campo de texto), todos os tipos de marcador (`{pct} {valor} {n} {data}
  {codigo} {nome} {cod} {desc}`), os quatro caminhos de escape da ferramenta, e
  o subtitulo da vitrine, que so e emitido quando preenchido.

Ela existe porque a de fabrica **nao dizia nada sobre o caminho configurado**:
com os textos no padrao, um texto que o gerador deixasse de emitir, ou que
escapasse errado, passaria sem acusar nada. Na rodada que criou os 162 campos
de texto, um marcador `{n}` chegou a ficar cru na tela do cliente -- a passagem
de fabrica via a declaracao da variavel e passava; so a configurada alcanca o
uso.

**Cada texto configurado leva um selo (`ZxNN`), e o script cobra que ele apareca
em alguma saida.** Selo que some e campo mal ligado, ou ramo que o cenario nao
percorre -- e ramo nao percorrido so e aceito quando esta **declarado**, com o
motivo, na quarta coluna da tabela `TEXTOS` em `cenario.mjs`. A declaracao
tambem e conferida ao contrario: se o ramo declarado voltar a aparecer, o script
avisa que a declaracao envelheceu.

## Exemplo minimo de uso do molde (`pagina.mjs`)

```js
import { comBlocoNaPagina, gerarNaFerramenta, chk, resumo } from './pagina.mjs';

// 1. gera o bloco na propria ferramenta
const { valores } = await gerarNaFerramenta(async pg => {
  // ...preencher campos e clicar no botao de gerar da aba...
}, ['b-out1', 'b-out2']);

// 2. executa o bloco gerado numa pagina de verdade
const r = await comBlocoNaPagina({
  cabeca: valores['b-out1'],                       // vai na Tag Head
  corpoAntes: '<div id="alvo">conteudo</div>',
  corpoDepois: '<scr'+'ipt>document.getElementById("alvo").style.cssText += ";' + valores['b-out2'].replace(/"/g,'\\"') + '";</scr'+'ipt>',
  medir: async pg => ({
    animacao: await pg.$eval('#alvo', el => getComputedStyle(el).animationName)
  })
});

// 3. confere e fecha
chk('animacao comeca com fc-borda-', r.animacao.indexOf('fc-borda-') === 0, r.animacao);
process.exit(resumo());
```

Um teste novo desta familia costuma ter esse formato: gerar com
`gerarNaFerramenta`, executar com `comBlocoNaPagina`, e um punhado de `chk(...)`
seguido de `process.exit(resumo())`. O molde nao sabe o que e uma "borda" ou
uma "cobranca" -- quem sabe e a funcao `medir` que cada teste escreve.

## O que este arnes NAO cobre

- **Nao substitui a conferencia visual.** Ele mede o que o codigo calcula
  (um valor, um nome de animacao, um texto), nao como a pagina fica na tela.
  Layout, espacamento e legibilidade continuam exigindo olhar.
- **Nao substitui o teste na pagina publicada.** Editor de Prosite e pagina
  publicada sao coisas diferentes (ver `CLAUDE.md` -- "editor != publicado"),
  e o sanitizador do Prosite (Manual do Prosite, em
  `docs/documentacao-fotocerta.md`) so age quando a pagina e de fato
  publicada. Um bloco que passa aqui ainda precisa ser colado e publicado
  para confirmar que o sanitizador nao mordeu nada.
- **`geradores.mjs`/`regressao.sh` comparam TEXTO, nunca executam o bloco.**
  Para conferir comportamento (anima, calcula, reage a clique) o teste
  precisa ser escrito com `pagina.mjs` -- e `textos-escape.mjs` e o exemplo
  vivo disso para os textos configuraveis.
- **O detector de acento nao alcanca tudo, e ele mesmo diz o que.** As cinco
  faixas estao escritas no cabecalho do `acentos.mjs`; as duas que mais pesam:
  palavra cuja forma ACENTUADA nao existe em lugar nenhum da arvore (nao ha com
  o que comparar) e HOMOGRAFO (`e`/`é`, `esta`/`está`, `pode`/`pôde`), que ele
  perdoa sempre -- inclusive quando o acento faltava mesmo. A "irma acentuada"
  (frase igual a outra a menos dos acentos) pega parte desses; o resto continua
  dependendo de leitura humana.
- **A INTERFACE da ferramenta fica quase toda fora.** `geradores.mjs` preenche
  campos e le saidas; ele nao confere se um campo da tela gravou, se uma lista
  mostra o que guarda, ou se um alerta esta acentuado. `lista-cupons.mjs` cobre
  um pedaco disso (o editor em linha das listas de cupons),
  `preset-formulario.mjs` outro (o formulario de cadastro na recarga, no
  "Aplicar" e num estado gravado sem a chave `form`) e `duplicar-itens.mjs` um terceiro (os botoes de duplicar das
  seis listas que os ganharam, e a exclusao de familia com o conteudo dela)
  porque foi ali que os defeitos apareceram; o resto
  continua dependendo de leitura e de olhar.
- **O cenario nao percorre todo ramo da ferramenta.** Os ramos que a passagem
  configurada declara hoje como fora dele: o formato de data da pagina de
  obrigado do TidyCal (o cenario mantem "como o TidyCal mandar"), o modo SINAL
  do **Checkout** (ver abaixo), o botao de fechar da Contagem regressiva (o
  padrao e "Nao ter") e o marcador `{prazo}` nas mensagens dela. Ligar qualquer
  um deles na passagem de FABRICA mudaria a passagem que prova o invariante --
  entao a escolha e consciente, e esta escrita em `cenario.mjs`.
- **O SINAL entrou na regressao em 13/09/2026, pela Mini loja.** Ate ali ele nao
  era alcancado por passagem nenhuma. Medido no dia: com a divida D1 ja aplicada
  -- que troca a linha do saldo no WhatsApp nas DUAS abas --, a regressao contra
  `main` deu **zero divergencia**; zero ali nao significava "nada mudou", e sim
  "o cenario nao chega la". Com `ramoSinal()` ligando o sinal da Mini loja na
  passagem CONFIGURADA, o `m-out` dela passou de 46.196 para 49.048 bytes -- sao
  ~2,8 KB de codigo que so agora estao sob a rede byte a byte -- e a mesma
  regressao passou a acusar a divergencia de D1, em uma linha. **So a Mini loja,
  e a razao e medida:** ligar o sinal do Checkout apagaria a cobertura de
  `u-txt-zap-valor` (Zx10), que so existe no ramo SEM sinal da mesma aba; os dois
  ramos de uma aba nao cabem numa passagem so. O que fica de fora -- o que e
  proprio do Checkout com sinal, o tipo "valor fixo" e as tres recusas -- e
  coberto por `sinal.mjs`, com os blocos rodando.
- **O sinal da aba Agendamento por pacote tambem fica fora da regressao, e de
  proposito.** Ele nasceu em 13/09/2026 (rodada C) com o padrao de fabrica
  DESLIGADO, e o contrato daquela rodada era justamente esse: com o interruptor
  desligado o bloco tem de sair **byte a byte** igual ao de `main` -- medido, as
  25 saidas x 2 passagens deram zero divergencia. Liga-lo no cenario mudaria a
  passagem que prova o invariante. O caminho COM sinal daquela aba e coberto por
  `sinal.mjs`, com o bloco rodando, em oito configuracoes.
- **Os campos de texto nascidos em 13/09/2026 ainda nao estao na tabela TEXTOS.**
  Sao o `*-txt-zap-saldo` (divida D1), os quatro do sinal da aba Agendamento por
  pacote (`a-t8`, `a-t9`, `a-txt-sinal-maior`, `a-txt-sinal-zero`) e os **dez do
  "Ja paguei"** da mesma aba (`a-txt-zap-pago`, `-abertura`, `-abertura-sinal`,
  `-pedido`, `-cupom`, `-descpix`, `-total`, `-sinal`, `-saldo`, `-valor`).
  Nenhum existe em `main`: poe-los na tabela agora faria `configTextosAusentes`
  encher e a passagem configurada inteira deixar de ser comparavel contra a
  referencia. Entram na rodada seguinte a esta ser mesclada. Os quatro do sinal e
  sete dos dez do "Ja paguei" ja sao exercitados com texto proprio por
  `sinal.mjs`, com o bloco rodando -- os tres que faltam (`-descpix`, `-total`,
  `-sinal`) sao lidos la com texto proprio, mas sem valor hostil, porque os dois
  ramos em que eles vivem ja sao percorridos pelos outros sete.
- **Os doze textos que explicam o sinal ficam fora da regressao POR CONTRATO.**
  Eles nascem vazios, e vazio nao emite nada: a saida com a configuracao de
  fabrica e byte a byte a de antes da rodada E, e e isso que `regressao.sh`
  mede. Poe-los na tabela `TEXTOS` faria a passagem configurada deixar de ser
  comparavel contra `main` (nenhum deles existe la) **e** apagaria o invariante
  que a rodada entregou. Quem os mede e `textos-sinal.mjs`, com os blocos
  rodando, em quatro estados de texto.
- **Sem service worker, sem PWA de verdade.** O servidor de `pagina.mjs` e
  de uma rota so, so para hospedar o bloco sob teste.
- **Tres arquivos daqui falam com a internet, e so tres.** `pagina.mjs` aborta toda
  requisicao que nao seja do proprio servidor, de proposito -- teste que
  depende de CDN falha por rede e o erro nao teria nada a ver com o bloco. A
  excecao esta declarada em `permitir` (hosts) e `bloquear` (pedacos de URL), e
  quem a usa sao o `tidycal-unificado.mjs`, o `corrida-calendario.mjs` e as partes 3 a 6 do
  `calendario-aquecido.mjs` (as partes 1, 2, 7 e 8 dele correm com a rede fechada):
  so o TidyCal de verdade sabe dizer se
  a altura acompanha o conteudo e se o formulario de reserva abre espaco, e uma
  biblioteca de terceiro pode mudar amanha. Esse arquivo PODE falhar por rede, e
  quando falhar a leitura certa e "nao mediu", nao "quebrou".
  O `tidycal-altura.mjs` continua provando o bloco contra sinais **transcritos**
  de uma medicao -- os dois se completam: aquele cobre a faixa da guarda e os
  casos que o calendario real nao produz sob encomenda (altura absurda,
  negativa, de outra origem); este cobre o comportamento contra o servico vivo.
